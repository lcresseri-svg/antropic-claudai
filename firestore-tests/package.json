{
  "name": "sunny-firestore-tests",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Firestore security-rules tests (Firebase emulator). Not deployed.",
  "scripts": {
    "test": "vitest run",
    "test:emulator": "firebase emulators:exec --only firestore --project sunny-test \"vitest run\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "vitest": "^4.1.7"
  }
}
