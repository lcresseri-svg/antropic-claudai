# Changelog — Sunny

Log condiviso delle modifiche fatte dagli assistenti AI che lavorano su questo
repository (**Claude Code** e **Codex**). Serve a entrambi per vedere cosa è
stato cambiato, quando e perché, senza dover ricostruire tutto dalla cronologia
git.

## Come aggiungere una voce

- Aggiungi le modifiche **in cima**, sotto la data di oggi (formato `AAAA-MM-GG`).
- Crea una nuova sezione data solo se non esiste già.
- Ogni voce: `**[Agente]** descrizione sintetica` + hash del commit tra backtick.
  - Agente = `Claude` oppure `Codex`.
- Registra solo modifiche significative (feature, fix, refactor, UI), non i
  commit di solo formato o WIP.
- Aggiorna questo file **nello stesso commit** della modifica che descrive.

---

## 2026-08-24

- **[Claude]** **Movimenti raggruppati per giorno (punto 5 del design handoff).** La lista non è più "un mese, trenta righe datate": ora ha due livelli. **Intestazione di mese** (nuovo `MonthSummaryHeader`): nome del mese, "13 movimenti · 3 programmati", il netto a destra e una **barra a segmenti** entrate/uscite/investito con la sua legenda — costruita su `aggregateFlow`, la stessa scomposizione del flusso unificato della home. **Card per giorno**: intestazione "Oggi · 24 agosto" / "Ieri · 23 agosto" / "7 agosto" con il totale del giorno, e sotto le righe in una card raggio 18 con divisori hairline. I giorni fatti di soli movimenti previsti diventano il gruppo **"Programmato · 28 agosto"**: intestazione oro, card a bordo tratteggiato, importi in `--text-2` e meta che dice "fra 4 giorni". Il raggruppamento sta in un modulo puro `listGrouping.ts` (`buildMonthSections`, `dayLabel`, `relativeDayLabel`, `daysBetween` in UTC così il cambio di ora legale non sposta il conteggio) con **7 test**; non riordina nulla — segue l'ordinamento scelto dall'utente — con la sola eccezione dei giorni interamente programmati, che chiudono il loro mese. **Header 56px** dentro la lista (il titolo esce da `AppRoutes`): titolo, **ricerca che è un'icona** e si apre in campo, e selezione multipla. **Una sola riga di filtri** scorrevole: chip attivo PIENO con il conteggio ("Tutte · 71"), poi i tipi da `TYPE_META`, poi il chip "Filtri" che apre il pannello di sempre (ordina/raggruppa/periodo/previsti) — invariato nel contenuto, più il nuovo "Per giorno" e il "Comprimi tutti" che prima stava nella riga rimossa. Il pannello vive fuori dallo scroller orizzontale, altrimenti l'overflow lo taglierebbe. `TransactionRow` guadagna `hideDate`: nel gruppo per giorno la data è ridondante e al suo posto la meta porta l'equivalenza annuale della serie in forma compatta (nuovo `formatSeriesEquivalentCompact`: "9.000,00 €/anno" invece di "≈ 9.000,00 € / anno", sei caratteri che su 390px sono la differenza fra leggere la cifra e vedere "≈ 9.000,…"). **Un'incoerenza trovata e corretta strada facendo**: la barra del mese filtrava per `!projected` mentre il netto usava `!isUpcoming`, quindi le uscite pianificate finivano nella barra ma non nel netto e i due numeri non tornavano (1.974 vs 2.954 nei dati di prova); ora la lista passa alla card le righe realizzate già filtrate, e il tipo lo impone. Su desktop la lista si ferma a `max-w-3xl`: una riga larga 1900px metterebbe descrizione e importo agli antipodi. Verifica visiva con Playwright a 390px e 1280px, chiaro e scuro. `tsc` pulito, build OK, **592 unit test** verdi (7 nuovi). `(pending)`

- **[Claude]** **Tab Patrimonio + nav (punto 4 del design handoff).** Nuova route **`/wealth`** e nuova voce di navigazione: in `BottomNav` e `SideNav` **"Consigli" lascia il posto a "Patrimonio"** (icona a linea spezzata, la stessa del handoff), e i consigli restano raggiungibili dalla "prossima mossa" in home e — su desktop — dalla sidebar, che li tiene in coda nel nuovo ordine Oggi · Patrimonio · Piano · Movimenti · Consigli · AI Coach. `WealthScreen` **compone, non riscrive**: hero con patrimonio, variazione a 3 mesi e andamento a 12 mesi (`buildWealthHistory` + `WealthLineChart`, la stessa definizione di patrimonio della schermata "Andamento patrimonio"), le due componenti liquidità/investito con la quota sul totale, e poi `AccountsCard` e `InvestmentSummaryCard` **così com'erano**, spostate qui dal fondo della home. La serie segue `includeInvestments` come fa `netWorth`, così il numero in cima e la linea non possono divergere. Le due righe di navigazione della card Patrimonio in home ora puntano a `/wealth` invece che alle singole schermate. Due rifiniture alle card riusate: la legenda di `InvestmentSummaryCard` perde la colonna percentuali (su 390px mangiava il nome della categoria, che si leggeva "A…", e la proporzione la dice già l'arco) e la torta scende a 118px come da design; il CTA "Aggiungi" della sidebar diventa pieno oro come il FAB. Su desktop due colonne indipendenti in altezza, coerenti con la home. Verifica visiva con Playwright a 390px e 1280px, chiaro e scuro. `tsc` pulito, build OK, **585 unit test** verdi. `(pending)`

- **[Claude]** **v2.0.0 fuori dalla beta + popup "Sunny ha un nuovo aspetto".** `APP_VERSION` 1.18.0 → **2.0.0** e `APP_CHANNEL` `beta` → **`stable`**: il badge "beta" in Impostazioni e l'avviso in cima al registro versioni erano già condizionati a quel valore, quindi spariscono da soli senza toccare la schermata. **Popup unificato**: `WhatsNewModal` e `ReleaseNotice` erano due scaffold quasi identici (stessa struttura, stessa lista puntata, stesso CTA); ora la presentazione è tutta in un solo componente `shared/components/ReleaseDialog.tsx` e i due file restano wrapper sottili con la SOLA logica del "già visto", che è davvero diversa (per-versione in `localStorage` vs per-utente via `releaseNoticeStorage`). Il popup nuovo non è più un changelog: pill versione + data, titolo in DM Serif 28px, sottotitolo, **al massimo tre voci in evidenza** (icona 34px + titoletto + spiegazione, divisori hairline), il resto collassato dietro "Altre N modifiche · Mostra ⌄", e un **CTA pieno oro che porta dove la novità si vede** con "Ho capito" come azione secondaria e il link al registro versioni sotto. `VersionEntry` guadagna tre campi **opzionali** (`subtitle`, `highlights[]`, `primaryAction`): senza `highlights` il dialog ricade sulla lista puntata di `changes` e mostra il solo "Ho capito" pieno — **nessuna regressione sulle release già pubblicate** (l'avviso `investments-flow-2026-07` è passato al nuovo componente senza cambiare aspetto né logica). La regola "mai due popup insieme" ha ora anche una rete di sicurezza al montaggio: `isWhatsNewPending()` viene letta **una volta sola** in `App.tsx` (`useState(isWhatsNewPending)`), così se la release in evidenza deve ancora essere vista l'avviso per-utente aspetta la prossima apertura invece di comparire appena chiudi il primo. Contenuto della voce 2.0.0: le tre cose che si vedono (liquidità libera, ritmo del mese, home più asciutta) più due collassate (profondità delle card, uscita dalla beta); il CTA porta alla home. Chiusura da backdrop, ✕, Esc e da entrambe le azioni, con il "già visto" marcato in ogni caso — come prima. Verifica visiva con Playwright sul CSS compilato a 390px e 1280px, in chiaro e scuro: popup completo dentro lo schermo del telefono, espansione delle voci collassate e chiusura da Esc. `tsc` pulito, build OK, **585 unit test** verdi. `(pending)`

- **[Claude]** **Redesign home "Oggi" — punti 1-3 del design handoff (v2.0.0).** **(1) Elevazione e animazioni.** Le card erano piatte per scelta (`boxShadow.card: 'none'`): la separazione veniva solo dalla hairline. Nuovi token in `index.css`, uno per tema, con i due soli livelli previsti — `--elev-2` per la card **hero** di una schermata (una sola) e `--elev-1` per tutte le altre, mai sulle righe interne — più la superficie hero (`.hero-card`: gradiente caldo e bordo alto oro in scuro, superficie piena `#FFFDF9` in chiaro) e la superficie calda a bordo oro (`.accent-card`, usata da "Prossima mossa" e dalla testa del popup Novità). In `tailwind.config.js` `shadow-elev-1`/`shadow-elev-2` puntano ai token. Tre `@keyframes` (`riseIn`, `growBar`, `drawRing`) con l'easing già in uso, applicati via classi (`animate-rise-in`, `.bar-grow`, `.ring-draw`) e mai ricalcolati a ogni render; `prefers-reduced-motion` li collassa con la regola che c'era già. Due sole coppie di token *nuovi* oltre a quelli del handoff, entrambe perché **si invertono** fra i temi e altrimenti sarebbero finite come hex nei componenti (con le relative regole `html.light .text-[#…]`): `--rhythm-ink`/`--rhythm-ink-hi` per il numero dentro la cella del calendario e `--accent-surface` per la superficie calda. **(2) `MonthRhythm`.** Calendario a 7 colonne del mese corrente (lunedì in prima colonna) al posto dell'idea di heatmap compressa: un quadratino per giorno, intensità = spesa del giorno / spesa massima del mese su 5 gradini di alpha sull'oro, giorni futuri neutri, giorni **programmati** a tratteggio oro, oggi con outline. Il calcolo sta in un modulo puro `monthRhythm.ts` (`buildMonthRhythm`) coperto da **8 test**: aggrega `ownShare` e scarta i movimenti ancora `isPending`, esattamente come il resto della dashboard. Il "programmato" non viene ricalcolato lì dentro — arriva già proiettato e deduplicato da `computeAvailableCash`/`commitmentProjection`, la **stessa lista** che alimenta la liquidità libera dell'hero, così il calendario e il numero in cima non possono raccontare due storie diverse. Il tap su un giorno apre i movimenti di quella data: nuovo filtro `?date=YYYY-MM-DD` in `TransactionList`, con la sua pill di rimozione, accanto a `?cat=`/`?account=`/`?series=` già esistenti. **(3) Home mobile e desktop.** Il numero in cima non è più il patrimonio ma la **liquidità libera** (liquidità − impegni entro fine mese, riserva esclusa: quella è un concetto della schermata "Liquidità disponibile"), con l'anello della quota ancora libera e i tre valori del mese; sotto, patrimonio netto con sparkline a 3 mesi — da `buildWealthHistory`, seguendo `includeInvestments` come fa `netWorth` — e due righe di navigazione, poi ritmo del mese, "dove vanno i soldi" (donut + legenda a 5 voci, con la coda raccolta in "Altre N") e una sola **"prossima mossa"** presa dal motore insight (primo `urgent`, poi `alert`/`forecast`). Escono da `DashboardV2` `TrendChart`, `AIDigestCard`, `InsightTicker`, le quattro `MonthStatCard`, `AccountsCard`, `InvestmentSummaryCard` e `CategoryCard`: i contenuti restano raggiungibili dalle righe di navigazione e dai link "Tutte". Su desktop due colonne indipendenti in altezza (hero + Ritmo|Torta a sinistra, patrimonio + prossima mossa + **ultimi movimenti** nei 352px a destra) e la riga di testa con saluto, contesto del mese e scorciatoia al riepilogo del mese scorso; su mobile il saluto nell'header lascia il posto a "Agosto · giorno 24 di 31" (solo sulla home, `monthContext` in `utils.ts`) e il FAB della bottom nav diventa pieno oro (`.cta-gold-fill`). **Nota**: il tab Patrimonio è il punto 4 dell'ordine e non c'è ancora, quindi le due righe di navigazione puntano per ora alle schermate esistenti `/account-balance` e `/investments` — nessun contenuto è diventato irraggiungibile. Il redesign va a **tutti** gli utenti, non dietro flag: la release lo annuncia esplicitamente, e la proiezione degli impegni è matematica pura e locale sui dati che l'utente ha già (i flag `commitments`/`wealth_v2` restano quelli delle rispettive schermate). Verifica visiva con Playwright sul CSS compilato a 390px e 1280px, in chiaro e scuro. `tsc` pulito, build OK, **585 unit test** verdi (8 nuovi). `0c074be`

- **[Claude]** **Impegni/Liquidità: copy, accesso e segnali (admin-only).** Quattro rifiniture. **(1)** Via il copy "Anteprima admin" dal sottotitolo di `CommitmentsScreen`: resta il neutro "Una voce per serie, nessun doppione" (la schermata è già invisibile ai non-admin, dirlo due volte non aggiunge nulla). **(2)** In `SettingsScreen` la voce diventa **"Impegni (admin)"**, allineata a "Metriche (admin)"; il gate resta `isFeatureEnabled('commitments', user)` — con il flag a stage `admin` è *equivalente* a `isAdminUser`, ma passa dal registro centrale invece di aggiungere un check UID sparso (è la regola che `featureRollout.ts` si è data, e che permette di avanzare lo stage senza toccare la schermata). **(3)** **Disponibile negativo** non è più solo un numero rosso: accanto al valore compare l'icona ⚠️ e sotto una riga di avviso `bg-red/10 text-red` (`role="status"`) che dice **di quanto** gli impegni superano liquidità e riserva e cosa farci — trattamento coerente con il tono `caution`/`ACCENT.warn` degli insight (icona + testo, mai il solo colore: il colore da solo non arriva a chi non lo distingue). **(4)** **Autonomia non calcolabile**: il motivo era solo in fondo, nella lista delle spiegazioni; ora `computeAvailableCash` espone `autonomyUnavailableReason` (breve, minuscolo) e la riga mostra "— (servono mesi completi di storico spese)". Il motivo distingue due casi diversi che prima collassavano nello stesso testo: **nessun mese completo** di storico vs **mesi completi senza uscite proprie** (es. tutte spese condivise a carico di altri); la riga di spiegazione in fondo ora deriva dallo stesso campo, quindi non possono più divergere. 3 nuovi test sui motivi (assenza di storico, storico senza uscite proprie, nessun motivo quando l'autonomia c'è). `tsc` pulito, build OK, **577 unit test** verdi. `(pending)`

- **[Claude]** **Riga impegno cliccabile + posizione nel piano rate (admin-only).** In `CommitmentsScreen` la riga intera di abbonamenti/rate/ricorrenti è ora un bottone (`min-h-[44px]`, focus e tastiera gratis, `aria-label` "Vedi i movimenti di …") che porta ai movimenti della serie sul suo conto: `/transactions?account=<id>&series=<seriesId>`. Nessun parametro inventato — `?account=` e `?series=` erano **già** supportati da `TransactionList` e si combinano in AND; se la serie non ha un conto (versamenti senza conto) il filtro conto viene semplicemente omesso. Per arrivarci `Commitment` espone due campi **derivati** (nessun campo nuovo nel modello dati, niente scritture): `account` (dal template, in fallback dall'ultima occorrenza registrata) e la posizione nel piano `paidInstallments`/`totalInstallments`, calcolata da `totalInstallments − remainingInstallments` che `buildSeriesSummary` già forniva — in riga diventa "rata 3 di 24 · 22 rate residue · … da pagare". Le serie non a rate non espongono nessuna posizione. 1 nuovo test (conto della serie, posizione nel piano, assenza dei campi sulle serie semplici). `tsc` pulito, build OK, **575 unit test** verdi. `(pending)`

- **[Claude]** **Riserva di sicurezza persistita (admin-only).** La riserva della "Liquidità disponibile" era uno `useState(500)` dentro `WealthV2Screen`: si azzerava a ogni ricarica e non seguiva l'utente fra dispositivi. Ora è un campo su `meta/settings` (**nessuna nuova collection**): `cashReserve` nel `SettingsProvider` con lo stesso pattern degli altri campi — letto dallo snapshot (con fallback al default storico di 500 € quando assente o non valido), scritto in `merge` con la sola chiave sua, riportato al default al logout. Il campo numerico della schermata resta locale mentre si digita (il calcolo risponde subito) e salva **in debounce a 600 ms**, così "1500" non produce quattro scritture Firestore; il valore persistito resta la fonte di verità e riallinea il campo quando cambia da fuori (primo snapshot, altro dispositivo). `firestore.rules`: `cashReserve` accettato solo come numero >= 0, con 4 nuove asserzioni nel test regole di `meta/settings` (0 e 1500 ok, −1 e la stringa `'500'` rifiutati). `tsc` pulito, build OK, **574 unit test** verdi + **35 test regole** su emulatore. ⚠️ tocca rules → deploy da CI al merge. `(pending)`

- **[Claude]** **Impegni e Liquidità disponibile sulla stessa proiezione (admin-only).** Le due schermate calcolavano per conto proprio l'insieme delle uscite future: `commitments.ts` guardava solo la prossima scadenza dei template attivi, `availableCash.ts` sommava template + occorrenze proiettate + una-tantum pianificate. Stesso dataset, stesso orizzonte, due liste diverse — e nessun test che le tenesse allineate. Nuovo modulo puro **`commitmentProjection.ts`** (`buildCommitmentEvents` + helper `addDaysISO`): data la lista dei movimenti e l'orizzonte, restituisce le occorrenze future **deduplicate per (serie|data)** dalle tre sorgenti (prossima scadenza del template attivo, occorrenze successive proiettate in memoria, uscite una-tantum future), sempre con l'**importo pieno** e un flag **`shared`**; ogni evento porta con sé la riga sorgente, così chi somma applica `ownShare` senza duplicarne la logica. I due consumatori ora si distinguono solo per COME sommano: Impegni somma l'importo pieno (numeri invariati), Liquidità disponibile la sola quota propria (numeri invariati). 4 nuovi test in `commitmentProjection.test.ts`, fra cui quello che verifica che `buildCommitments().upcoming` e `computeAvailableCash().committedItems` siano **la stessa lista di eventi prima della somma** (stessi elementi, stesso ordine) e che la differenza fra le due somme sia esattamente la quota altrui. `tsc` pulito, build OK, **574 unit test** verdi. `(pending)`

---

## 2026-08-22

- **[Claude]** **Storni / rimborsi di spese (v1.18.0).** Nuovo `type: 'refund'` dedicato (niente abuso di `income`/`transfer`) legato alla spesa via **`refundOf`**; più storni per spesa, totali o parziali. **Il perno è la doppia vista con un solo punto di innesto**: `ownShare` diventa la spesa NETTA (`amount − shared − refundedTotal`) e resta il default per TUTTE le statistiche, mentre il nuovo **`grossOwnShare`** (lordo) serve solo a cassa e saldi. `refundedTotal` è un campo DERIVATO client-side (come `projected`, mai persistito: rimosso in `buildPayload` **e** in `plainWrite`) scritto da **`applyRefunds`**, invocato una sola volta in `useTransactions` — così categorie, budget, insight, forecast, recap, trend e `monthlyAggregates` vedono la spesa netta **senza che nessuno di loro conosca gli storni** (zero modifiche ai ~20 call-site di `ownShare`). La spesa originale non viene mai toccata: lo storico del cashflow resta fedele. **Attribuzione**: lo sconto cade nel mese della SPESA anche se il rimborso arriva mesi dopo; la cassa invece resta sulla data reale dello storno (`accountDelta`/`liquidityDelta` → `+amount` sul conto scelto). Nel flusso unificato lo storno è un componente **suo** (`refundsReceived`, dentro `cashIn` ma fuori da `ordinaryIncome`), esposto nel ⓘ Entrate della dashboard e nei subtotali dei Movimenti. Nuovo modulo puro `shared/refunds.ts` (`isRefund`, `buildRefundIndex`, `applyRefunds`, `refundsFor`, `summarizeRefunds`, `refundableExpenses`): esclude righe projected e storni con data futura, e **impedisce storni cumulativi oltre l'importo** (`remaining`, con `excludeRefundId` perché in modifica lo storno non conti contro sé stesso). **UI**: nuova `RefundSheet` sul `SheetShell` esistente (spesa originale, importo, data, conto di accredito, nota, preview "Spesa dopo lo storno"), raggiungibile dal dettaglio spesa ("Registra storno" + elenco storni, totale stornato e spesa effettiva) e dal `+` ("Registra storno di una spesa" → scegli la spesa); nei Movimenti riga a sé `+XX €` con badge "↩︎ Storno" e colore proprio (mai il verde delle entrate), e sulla spesa la riga "stornati …". Modifica/eliminazione ricalcolano da sé (edit in place, snapshot); eliminando una spesa si eliminano anche i suoi storni, che sarebbero orfani. **Disambiguazione**: lo "storno" preesistente delle spese condivise (la quota altrui che rientra, un `transfer` con `groupId`) è stato rinominato in **quota/settlement** — identificatori (`storni`→`settlements`, `groupStorni`→`groupSettlements`), commenti, label UI e prefisso dei NUOVI documenti (`Storno · ` → `Quota · `; i documenti legacy restano invariati). Allineate le Cloud Functions (`sendMonthlySummary`: storno in `cashIn`, non entrata; `remindMonthEnd`: scala le spese del mese) e `firestore.rules` (`type` accetta `refund`, `refundOf` stringa ≤128). **21 nuovi test** in `refunds.test.ts` (indice, netto vs lordo, attribuzione al mese della spesa, cassa alla data reale, limiti cumulativi, selezione spesa) più 3 di integrazione che verificano che la spesa netta arrivi davvero fino a `aggregateCategorySpending` e `buildMonthlyAggregates`, e 1 nuovo test regole. `tsc` client+functions puliti, build OK, **570 unit test** verdi + **35 test regole** su emulatore. ⚠️ tocca functions e rules → deploy da CI al merge. `(pending)`

- **[Claude]** **Popover ⓘ: chiusura, impilamento e lato di apertura (v1.17.3).** Tre difetti segnalati con screenshot, tutti nello stesso componente. **(1) Restava aperto** (e se ne potevano avere due insieme): il velo di chiusura di `OutflowInfo` è un `div`, e `MonthStatCard` avvolge il contenuto in `pointer-events-none [&_button]:pointer-events-auto` — `pointer-events` è **ereditata**, quindi il velo (non essendo un `button`) non riceveva più il tap e il tap-fuori non chiudeva nulla. Ora velo e pannello dichiarano `pointer-events-auto` espliciti: il componente non dipende più da cosa fa l'host. **(2) Sembrava trasparente**: `bg-elevated`/`glass-card` sono tinte piene senza blur, quindi non era alpha ma **ordine di disegno** — il wrapper `relative z-10` della card creava un contesto di impilamento che intrappolava il pannello `z-50` dentro la card, e la card successiva della griglia (anch'essa `z-10`, dopo nel DOM) gli finiva sopra: per questo il ⓘ di "Entrate" era coperto dalla card "Uscite" mentre quello di "Investimenti", che si sovrappone a "Flusso netto" (card senza freccia, quindi senza `z-10`), si vedeva bene. Tolti gli `z-*` dai wrapper: senza z-index l'ordine di disegno segue il DOM (il contenuto copre comunque il bottone che lo precede) e il pannello sale sopra le altre card. **(3) Usciva dallo schermo a destra**: il ⓘ sta spesso subito prima di un importo allineato a destra (subtotali in Movimenti, righe conto), e l'ancoraggio era fisso a `left-0`; ora il lato si sceglie misurando lo spazio reale — nuovo helper puro **`pickAnchor(iconLeft, viewportWidth)`** (+ costanti `PANEL_W`/`EDGE_GAP`) che ribalta a `right-0` quando il pannello non ci sta. Aggiunto `whitespace-nowrap` agli importi, così una cifra non si spezza mai su due righe. 4 nuovi test su `pickAnchor` (spazio disponibile, ribaltamento al bordo, soglia esatta, schermo strettissimo). Verifica visiva e di hit-testing con Playwright sul CSS compilato a 390px: `elementFromPoint` conferma che il pannello è in cima sopra la card accanto, che il tap fuori colpisce il velo e che la ⓘ resta raggiungibile entro 8px in ogni direzione senza che l'overlay "apri" glielo rubi. `tsc` pulito, build OK, **549 unit test** verdi. `(pending)`

- **[Claude]** **Categorie di spesa "a finanziamento" — admin-only (`expense_financing`, stage `admin`).** Nuova gestione per tracciare un prestito/finanziamento su una categoria di spesa: `CategoryDef.financedAmount` (importo prestato) impostabile in `EditDefSheet` solo per categorie `expense` e solo dietro il flag centrale `expense_financing` (nuovo in `featureRollout.ts`, registro `admin → allowlist → percentage → all`, come `wealth_v2`/`commitments`/`monthly_plan_v2` — **non** un check `isAdminUser` sparso). Aprendo il dettaglio di quella categoria (tap sulla riga in "Spese per categoria" → `CategoryDetailSheet`) compare in cima una card **"Finanziamento"** con barra di progresso, **quanto versato finora** (somma storica delle spese reali di quella categoria, mai `projected`, mai datate nel futuro) e **quanto manca da restituire** (`financedAmount − versato`, mai negativo), con badge "Ripagato ✓" a saldo azzerato. Nuovi helper puri in `categoryAnalytics.ts`: `totalPaidAllTime` (somma storica, esclude template e movimenti futuri) e `buildLoanProgress` (versato/residuo/percentuale/stato). `SettingsScreen.save()` persiste `financedAmount` solo per categorie `expense` (ripulito su cambio tipo, come gli altri campi opzionali). 4 nuovi test (`totalPaidAllTime`/`buildLoanProgress`: somma corretta, categoria inesistente, progresso parziale, finanziamento ripagato oltre il dovuto → residuo 0, mai negativo, percentuale mai oltre 100). Nessun bump `APP_VERSION`, nessuna voce in `appInfo.ts` VERSIONS, nessun annuncio: feature invisibile ai non-admin, dati privati per utente (nessuna modifica a `firestore.rules`: i documenti di categoria non hanno validazione per-campo, solo il cap di dimensione sull'array). `tsc` pulito, build OK, **545 unit test** verdi. `(pending)`

- **[Claude]** **Fix hitbox: l'icona ⓘ delle card non sfiora più l'apertura della schermata (v1.17.2).** Nelle card "Entrate"/"Investimenti" del mese (`MonthStatCard`), un tap non perfettamente centrato sull'icona ⓘ (`OutflowInfo`, 13×13px) cadeva sul pulsante "apri" invisibile che copre l'intera card (`absolute inset-0`), aprendo la schermata di analisi invece del popover di dettaglio — segnalato dall'utente. Fix minimo e mirato: il bottone dell'icona in `OutflowInfo.tsx` guadagna un'area di tocco invisibile più ampia (`span` figlio `absolute -inset-2.5`, rimosso dal flusso quindi **nessuno spostamento di layout**, l'icona visibile resta 13px) — un tap leggermente impreciso ora resta sull'icona invece di cadere sul pulsante sottostante. Nessuna modifica al pulsante "apri" stesso né alla sua area. `tsc` pulito, build OK, **545 unit test** verdi.

- **[Claude]** **Entrate nette nella schermata Entrate (v1.17.1).** Nuova statistica richiesta: sotto il totale del periodo la `IncomeScreen` mostra ora le **Entrate nette** = totale **al netto del capitale rientrato dai disinvestimenti** (soldi già tuoi che tornano indietro, non reddito), mentre le **plusvalenze realizzate restano dentro** perché sono guadagno effettivo. Quando nel periodo c'è almeno un disinvestimento compare la derivazione completa (entrate lorde − capitale rientrato = entrate nette), altrimenti una riga spiega che le entrate sono già tutte nette; se ci sono plusvalenze, un riquadro dedicato ne mostra importo, peso % sulle nette e la quota di entrate ordinarie residue. Nessun sistema parallelo: `aggregateIncomeStats` (già usata dalla schermata) guadagna i campi puri `netIncome` / `realizedGains` / `ordinaryIncome` — `netIncome` è per costruzione `agg.total`, che somma le sole `income` e quindi **non** contiene la gamba `out` dei disinvestimenti (movimento `investment`), mentre le plusvalenze sono `income` di categoria di sistema `__plusvalenza__` (nuova costante esportata `REALIZED_GAIN_CATEGORY`). Rimossa la vecchia riga "Include … di capitale rientrato", assorbita dalla derivazione; KPI rinominato in "Media netta al mese" (era già calcolato sulle nette). 2 nuovi test (nette con plusvalenza + disinvestimento; nette = totale senza disinvestimenti). `tsc` pulito, build OK, **541 unit test** verdi. `(pending)`

- **[Claude]** **Schermata Entrate + scorciatoie ">" dalle card del mese (v1.17.0).** Nuova **`IncomeScreen`** (`/income`, lazy come le altre): stessa impaginazione e stessi controlli di periodo di `CategorySpendingScreen` (1m/3m/6m/12m + navigatore avanti/indietro, sticky). Contenuti: **totale periodo** (= card Entrate: entrate ordinarie + capitale rientrato, con la riga che isola i rientri) e delta vs periodo precedente; **KPI** media al mese, fonti attive, entrata più alta, movimento medio (+ in modalità avanzata: n. movimenti, quota ricorrente, mesi con entrate, quota della fonte principale); **grafico andamento 12 mesi** (`AccountBalanceLineChart` in verde); **composizione** con `Donut` + legenda; **avviso di concentrazione** quando una sola fonte supera l'80%; **dettaglio per fonte** con importo, %, n. movimenti, media e delta. Nessun sistema parallelo: l'aggregazione riusa le strutture esistenti di `categoryAnalytics` — nuove funzioni pure `aggregateIncomeByCategory` (stessa forma di `aggregateCategorySpending`, riusa `CategorySpendingSummary`), `aggregateIncomeStats`, `aggregateIncomeTrend`, `capitalReturnedIn`, più il filtro `isIncomeIn` (esclude projected e template di serie). **Card del mese**: `MonthStatCard` accetta `onOpen` → chevron ">" in alto a destra e card cliccabile (overlay assoluto, il ⓘ resta interattivo e non naviga); Entrate → `/income`, Uscite → `/category-spending`, Investimenti → `/investments`; **Flusso netto** senza freccia. Le card di navigazione "Spese per categoria" e "Investimenti" restano in dashboard come prima. 6 nuovi test sulle aggregazioni entrate. `tsc` pulito, build OK, **539 unit test** verdi. `(pending)`

- **[Claude]** **4 card "Questo mese" + apporti senza conto negli Investimenti (v1.16.0).** **(1) Card**: `Entrate` (entrate ordinarie + capitale rientrato dai disinvestimenti) · `Uscite` (**solo** spese effettive) · **`Investimenti`** (TOTALE investito nel mese = dai conti + senza conto + TFR, disinvestimenti esclusi perché riportano cassa) · `Flusso netto`. Layout `grid-cols-2 md:grid-cols-4`: 4 affiancate su desktop, griglia 2×2 su telefono (verificato con screenshot Playwright a 390px/1280px sul CSS compilato). ⓘ Investimenti scompone dai conti / senza conto / TFR ed esplicita quanto *toglie liquidità*. **(2) Semantica del flusso** (`shared/financialFlow.ts`): gli **apporti senza conto escono da `cashIn`** — non toccano nessun conto, quindi non sono né entrata né uscita, esattamente come il TFR: `cashIn = entrate ordinarie + capitale rientrato`, `netFlow = variazione REALE della liquidità`. `externalContributions` resta un componente esposto (card Investimenti, decomposizione patrimonio). Aggiornati di conseguenza `netFlowDelta` (subtotali Movimenti), insight del mese (sforamento / risparmiato finora / fine mese, con i testi delle spiegazioni) e il riepilogo push delle Functions. **(3) Saldi**: nel dettaglio conto la stessa distinzione era già corretta per costruzione (`accountDelta`) ma nascosta in modalità "minimal" → ora **Entrate / Uscite (solo spese) / Investimenti sono sempre visibili**, con nota che spiega perché un versamento senza conto o la quota TFR non compaiono lì. Test aggiornati/aggiunti (flusso senza conto fuori da cashIn, ricomposizione del totale investito, quadratura del conto con TFR e senza conto). `tsc` client+functions puliti, build OK, **533 unit test** verdi.

## 2026-07-27

- **[Claude]** **CI verde: patch della vulnerabilità HIGH nelle dipendenze delle Functions.** Il job `functions-build` falliva su `npm audit --audit-level=high` (CI #58 e #59) — **non** per il codice ma per un'advisory pubblicata dopo l'ultimo CI verde del 17/07: `fast-xml-parser` 5.9.3 (**GHSA-8r6m-32jq-jx6q**, high — i DOCTYPE ripetuti azzerano i limiti di espansione delle entità), dipendenza transitiva via `firebase-admin`. Applicato `npm audit fix` **senza `--force`**: aggiornato solo `functions/package-lock.json` (`fast-xml-parser` 5.9.3 → 5.10.1, `body-parser` 1.20.5 → 1.20.6, `protobufjs` 7.6.4 → 7.6.5), `package.json` **intatto** — nessun cambio di dipendenze dirette, nessun breaking change (l'opzione `--force` avrebbe forzato `firebase-admin@14`, scartata). Restano 9 advisory *moderate* già presenti prima e sotto la soglia del gate. `npm audit --audit-level=high` esce 0, typecheck e build Functions OK. ⚠️ tocca functions → deploy da CI al merge. `(pending)`
- **[Claude]** **Card del mese senza caps + insight sul flusso unificato (v1.15.4).** **(1) Card Entrate/Uscite/Flusso netto**: via `label-caps` dalle etichette (`MonthStatCard`) — sentence case a 12px, "Flusso netto" su una riga (niente più wrap/troncatura), valore a 16px; il popover ⓘ ora forza `normal-case tracking-normal` così non eredita più il MAIUSCOLO quando è renderizzato dentro un'etichetta caps (bug visibile in screenshot: "SPESE / INVESTIMENTI DAI CONTI / TFR ESCLUSO"). **(2) Insight del mese sul flusso unificato** — chiudono l'incoerenza segnalata (card: flusso −140 €, insight: "Sforamento di 781,88 €"): `buildInsights` calcola `monthFlow = aggregateFlow(mese corrente)` e **Sforamento** (#1) + **Risparmiato finora** (#3) usano `netFlow`/`cashIn`/`cashOut` — i versamenti SENZA conto contano come **entrate** (apporti esterni), il TFR resta escluso, stessi numeri delle card; grafici e spiegazioni aggiornati. **(3) Fine mese stimato** (#2) su input cash-coerenti: entrate già incassate = `cashIn` (apporti/rientri inclusi), investimenti = SOLO quota non-TFR pagata dai conti (`investedFromAccounts`), media storica cash (`liquidityDelta`), `upcomingInvest` in vista cash (template senza conto → 0, quota TFR esclusa) — TFR e versamenti senza conto non "prosciugano" più la previsione. Nuovi test (versamento senza conto → nessuno sforamento e conta come entrata; TFR con conto escluso; sforamento reale scatta ancora). `tsc` pulito, build OK, **531 unit test** verdi. `(pending)`

## 2026-07-17

- **[Claude]** **Fix eliminazione movimenti offline: ripristinato il fallback per i non-investimenti (v1.15.3).** Regressione da PR #100: `deleteTransactionsSynced` e `replaceGroupSynced` forzavano la transazione **atomica per TUTTI** i movimenti (`runOps(..., true)`), avendo rimosso il fallback offline anche per i non-investimenti — così su rete assente/instabile l'eliminazione (e i restructure di gruppo, es. spesa condivisa) fallivano silenziosamente invece di degradare alla scrittura in coda. Ora entrambe accettano `mustSync` calcolato dai dati locali (come `patchTransactionsSynced`): un investimento su un lato qualsiasi → path atomico (revert esatto del controvalore, errore propagato con Retry nella form); altrimenti → **batch offline-safe** (nessun controvalore da toccare). Chiamanti aggiornati in `useTransactions` (`deleteTransaction`/`deleteTransactions`/`deleteAll`/`replaceGroup`). **Diagnosi**: nuovo test d'integrazione `investmentValueSync.emulator.test.ts` che esercita il vero sync layer contro l'emulatore Firestore **con le regole reali + Auth** — provato che il delete di un versamento d'investimento **senza conto** (anche con TFR, `statsSpreadMonths`, `subscriptionDate` e campi legacy come `countInvestmentsInExpenses`) funziona online: documento rimosso, `currentValue` ripristinato esatto, campo legacy preservato; e che il non-investimento passa dal batch offline-safe. Il file si auto-salta quando l'emulatore non è presente (CI invariata). `tsc` pulito, build OK, **528 unit test** verdi + **4 test emulatore** (regole reali) verdi. `(pending)`
- **[Claude]** **Popover ⓘ di Entrate/Uscite apre verso destra (v1.15.2).** Il breakdown `OutflowInfo` (dettaglio del flusso su Entrate/Uscite in dashboard, card conti e subtotali Movimenti) era ancorato `right-0` e si apriva verso sinistra: sulla card "Entrate" (la più a sinistra) usciva fuori dallo schermo. Ora è ancorato `left-0` (apre verso destra) con `max-w-[calc(100vw-2rem)]` per non sforare a destra su schermi stretti — coerente su desktop e mobile. Solo CSS del componente. `tsc` pulito, build OK. `(pending)`
- **[Claude]** **Dettaglio investimento: KPI "Media annua semplice" senza XIRR (v1.15.1).** Il KPI "Guadagno medio annuo" non usa più XIRR: nuova metrica **Media annua semplice** — `versato = initialBalance + versamenti effettivi (TFR e apporti senza conto inclusi)`, `prelevato = Σ incassi dai disinvestimenti`, `guadagno = controvalore + prelevato − versato` (commissioni escluse per definizione), `anni = giorni/365,2425`, `€/anno = guadagno/anni`, `%/anno = (guadagno/versato)/anni` — mostrata insieme ("+1.000 €/anno · +10,0%/anno") con colori coerenti positivo/negativo. **Le stesse basi valgono anche per il rendimento annualizzato**: XIRR spostato dai KPI alle **statistiche avanzate** come "Rendimento annualizzato (XIRR)", calcolato sugli stessi flussi reali (mai quote `statsSpreadMonths`), con **movimenti futuri e template esclusi** da tutte le metriche del modulo (anche capitale netto/versato/grafico) e **fallback `subscriptionDate` → primo movimento effettivo** anche per l'ancoraggio di `initialBalance` (rimosso il blocco "serve la data di sottoscrizione" quando esiste almeno un movimento). Con data/durata/capitale non validi: "—" con spiegazione (`no-current-value`/`no-start-date`/`no-capital`/`insufficient-duration`/`insufficient-data`). Tipi aggiornati (`PositionPerformance.simpleAnnualGain/simpleAnnualGainPct/simpleUnavailableReason`, `MetricUnavailableReason`; rimosso `avgAnnualGain`), anno civile 365,2425 uniforme. Test: 8 nuovi casi (esempio numerico 10.000→12.000 su 2 anni ≈ +1.000 €/anno · +10%/anno, prelevato nel guadagno, TFR/apporti nel versato, commissioni fuori dalla media semplice, esclusione futuri/template, fallback data, no-capital). `tsc` pulito, build OK, **528 unit test** verdi. `(pending)`

## 2026-07-16

- **[Claude]** **Investimenti e flussi più chiari (v1.15.0).** **(1) Flusso finanziario unico** — nuovo helper puro `shared/financialFlow.ts` (`accountDelta`/`investedDelta`/`flowParts`/`netFlowDelta`/`aggregateFlow`) usato da dashboard, saldi conto (`useTransactions`), analytics per conto (`accountAnalytics.signedDelta` ora delega), serie patrimonio (`wealthAnalytics`), subtotali Movimenti, recap (`monthStats.savings` = flusso netto) e AI digest: per ogni investimento `TFR = clamp(tfr??0, 0, amount)` **sempre escluso dal flusso** (ma incluso in investito/controvalore/patrimonio) e **mai a carico del conto**; deposito con conto → uscita per la sola quota non-TFR; deposito senza conto → apporto esterno in entrata; gamba `out` del disinvestimento → capitale rientrato in entrata (plus/minus/commissioni restano nelle loro transazioni, zero doppi conteggi); trasferimenti esclusi. Dashboard: "Entrate"=`cashIn`, "Uscite"=`cashOut`, "Risparmio"→"**Flusso netto**", ⓘ con breakdown su entrambe; card conti con soli movimenti reali (`getAccountMovements` filtra i movimenti a impatto zero). **Rimosso dalla UI il toggle "Conta gli investimenti nelle uscite"** (campo Firestore legacy ignorato, nessuna migrazione). **(2) Controvalore senza fallback** — per gli investimenti niente più degradazione a plain-write: transazione atomica movimento+controvalore **o nulla** (errore propagato); form (`TransactionModal`, `InvestmentDepositSheet`, `InvestmentWithdrawSheet`) con await della Promise, loading, blocco doppio submit, chiusura solo dopo commit e **Riprova** in caso di errore; scritture non-investimento restano offline-safe. **Investimenti con data futura**: nessun effetto alla scrittura, marker `valuePending` e **reconciler idempotente** all'avvio (`reconcilePendingInvestments`, ri-verifica dentro la transazione, race-safe multi-device); `lastValueUpdate` in Europe/Rome. Fix: la modifica di una categoria da Impostazioni non perde più `currentValue`/`lastValueUpdate`/`archived`. **(3) Distribuzione solo statistica** — nuovo campo `Transaction.statsSpreadMonths` (2–120) sui depositi una tantum, UI in InvestmentDepositSheet e TransactionModal (Nessuna/3/6/12/Personalizzata, esclusa per serie); helper puro `investmentStatsSpread.ts` (`spreadQuotas` al centesimo con residuo sull'ultimo mese, `monthlyInvestmentStats` per competenza fino al mese corrente); usata SOLO da trend/medie/insight investimenti (grafico "Andamento versamenti", insight ritmo/quota investita) — un solo movimento reale, saldi/flusso/forecast/recap sull'importo reale; badge "Distribuito su N mesi" nelle righe. **(4) Dettaglio investimento** — nuova `InvestmentDetailSheet` (bottom sheet mobile / drawer desktop, Escape+focus trap) al click su una posizione (rimossa l'espansione inline): KPI capitale netto, controvalore (+stantio), guadagno totale €/% (`controvalore + incassi − conferito − commissioni`, incassi ricostruiti da `valueDelta`/gruppi senza ricontare plus/minus), guadagno medio annuo, **rendimento annualizzato XIRR** money-weighted (`investmentPerformance.ts`: bisezione robusta + rifinitura Newton, TFR e apporti senza conto inclusi come capitale conferito, `initialBalance` alla `subscriptionDate`, "—" con dati insufficienti), durata; grafico del capitale versato ricostruito (mai storico di mercato inventato, nota su da quando esiste il controvalore); statistiche complete (lordi/netti, disinvestito, medio, quota statistica del mese, ultimi 12 mesi, frequenza, commissioni, realizzato/latente, TFR e %, peso, prima/ultima operazione, n. movimenti); 5 movimenti recenti (conto/"Apporto esterno", TFR, badge distribuzione) e CTA → `/transactions?investment=<categoryId>` (nuovo filtro con pill rimovibile). **(5) `CategoryDef.subscriptionDate`** — campo "Data di sottoscrizione" (mai futura) nella gestione categorie investimento, con fallback alla prima operazione quando `initialBalance=0` e annualizzato non disponibile quando `initialBalance>0` senza data; export JSON invariato (categorie esportate integrali), documenti legacy validi. **(6) Avviso release** — nuovo `RELEASE_NOTICE_ID` `investments-flow-2026-07` ("Investimenti e flussi più chiari", CTA "Ho capito", una sola volta per utente dopo il caricamento dati); la voce 1.15.0 del registro **non** è `highlight` → mai doppio popup con WhatsNew. **Rules**: `validTxn` accetta `statsSpreadMonths` (int 2–120) e `valuePending` (bool) + test emulatore (inclusi legacy senza nuovi campi). Docs (ARCHITETTURA) e `APP_VERSION` aggiornati. `(pending)`

## 2026-07-13

- **[Claude]** **Controvalore investimenti sempre allineato (sync atomico e idempotente).** Registrare/materializzare una transazione `investment` ora aggiorna il **controvalore della categoria** (`currentValue`): versamento (`direction` assente/`in`) → `+amount`; prelievo (`out`) → `−amount` con **clamp a 0** (sequenziale nella stessa run); `currentValue` assente parte da 0; `lastValueUpdate` aggiornato; `initialBalance`/`tfrAmount`/storico **mai toccati**. **Meccanica**: nuovo core puro `investmentValueCore.ts` (pianificazione revert/apply) + layer I/O `investmentValueSync.ts` — documento e `meta/settings.categories` committano in **una transazione Firestore**, con **stamp `valueEffect`** (delta effettivamente applicato) scritto atomicamente sul documento → **idempotenza** (mai applicato due volte) e **revert esatto** su modifica/eliminazione/cambio importo-direzione-categoria (si annulla prima l'effetto precedente, poi si riapplica il nuovo). **Copertura**: inserimento manuale (modale + sheet Investi/Disinvesti — rimossi i vecchi bump ad-hoc non atomici di `saveCurrentValue`), ricorrenze materializzate dal client (catch-up), ricorrenze create dalla CF `processRecurringTransactions` (ristrutturata in transazione per template: istanze + avanzamento template + controvalore in un solo commit, stamp sulle istanze). Il leg `out` del disinvestimento porta `valueDelta: −cash` esplicito così il sync riproduce esattamente `newCurrentValue` della sheet. **Legacy/compat**: documenti senza stamp (storico pre-feature, **import CSV**, dati demo) restano *unmanaged* — modifiche/eliminazioni non toccano il controvalore già riconciliato a mano; un documento che DIVENTA investimento entra in gestione. **Offline**: le transazioni Firestore richiedono rete → fallback alla scrittura semplice in coda senza stamp (movimento mai perso, documento resta unmanaged). **Rules**: `validTxn` valida i nuovi campi `valueDelta` (numero, cap anti-DoS) e `valueEffect` (`hasOnly` category/delta/appliedAt) + test emulatore. Rimosso il helper superato `investmentValueDeltas` e la API morta `updateTransaction`. `tsc` client+functions puliti, build OK, **462 unit test** (16 nuovi) + **32 Rules test** verdi. ⚠️ tocca functions e rules → deploy da CI al merge. `(pending)`

## 2026-07-12

- **[Claude]** **Consolidamento app + funzioni admin gated (v1.14.0).** **(A) Consolidamento per tutti:** una sola Dashboard/Insights/Budget ufficiale (rimossi i componenti V1 irraggiungibili, la route `/forecast-v2` e i flag sempre-true; il motore V2 resta come baseline nel V3 screen e nei backtest); `App.tsx` ridotto a bootstrap con shell in `src/app/` (route **lazy** → bundle principale 702→~360 kB, `ErrorBoundary` contro le schermate bianche, banner **offline/sync**, header e gestione modali estratti); Functions **divise in moduli** (recurring/notifications/ai/shortcuts/metrics/deletion/feedback/shared) con `index.ts` di solo re-export — nomi, regioni e schedule invariati (verificato sull'export compilato); Rules: `meta/{doc}` generico → regole **specifiche validate** legacy-compatibili (`theme:'system'` resta valido; `meta/aiCoach` read-only client; fallback owner-only per i doc non enumerati), `forecastSnapshots` validati (sempre create-only); veri `<button>` al posto di `span role="button"` (InvestmentsScreen), CSS `prefers-reduced-motion`, App Check client opzionale (`VITE_APPCHECK_SITE_KEY`) + verifica soft nelle functions AI (`APPCHECK_ENFORCE` opt-in), aggregati mensili derivati predisposti (`shared/monthlyAggregates.ts`, versionati/rigenerabili/fallback, zero scritture all'avvio); CI: typecheck+test+build frontend, typecheck+build functions, Rules test. **(B) Funzioni SOLO admin** (registro centrale `featureRollout.ts`, stadi deterministici admin→allowlist→percentuale→tutti): **Patrimonio V2** (`/wealth-v2`: Δ = risparmio netto + rendimento + rettifiche — rendimento 0 finché non c'è storico snapshot, mai inventato; composizione e freschezza controvalori; periodi 1M/3M/6M/1A/Tutto), **snapshot patrimoniali** (`wealthSnapshots/{YYYY-MM-DD}` Europe/Rome, idempotenti, backfill month-end con **dry-run** e qualità reale/stimato/mancante), **liquidità disponibile** (liquidità − impegni 7/14/30gg/fine-mese − riserva, autonomia, spiegazione, zero doppi conteggi), **impegni** (`/commitments`: abbonamenti/rate/ricorrenti, equivalente mensile, residui, fine prevista, scadenze 30gg), **ForecastService unificato** (contratto esplicativo su motori V3/V4: breakdown registrato/programmato/ricorrente/variabile/eccezionale/segnale-budget/residuo che somma SEMPRE alla stima; backtest vs mediana-3-mesi/media-robusta/ultimo-mese/stesso-mese-anno-prima con MAE/MdAE/bias/err.rel./coverage), **Piano mensile V2** (`monthlyPlans/{YYYY-MM}`: copia mese precedente o semina da ricorrenti+stagionalità, conferma esplicita, piano≠consuntivo≠forecast), **ranking insight V2** (impatto/urgenza/confidenza/novità/azionabilità, 6 domini, un solo insight prioritario), **Decision Coach deterministico** (acquisto/taglio/rinvio con liquidità residua, riserva, autonomia, rischio, dati mancanti dichiarati — l'AI spiega, non calcola). Docs allineate (README/ARCHITETTURA/HANDOFF + IMPLEMENTATION_PROGRESS.md). `tsc` client+functions puliti, build OK, **446 unit test** + **31 Rules test** verdi. ⚠️ tocca functions e rules → deploy da CI al merge. `(pending)`

## 2026-07-09

- **[Claude]** **Andamento patrimonio: la liquidità coincide sempre con la dashboard ("oggi" allineato).** La schermata `/wealth-history` calcolava "oggi" con la **data locale**, mentre la dashboard (`useTransactions`) usa la data **UTC** (`toISOString`): tra la mezzanotte italiana e le ~02:00 il patrimonio contava già i movimenti datati col nuovo giorno che per la dashboard erano ancora "Programmato" → liquidità (e totale) diverse tra le due schermate. Ora `wealthAnalytics` deriva "oggi" con la **stessa convenzione della dashboard** (`dashboardToday = toISOString().slice(0,10)`) per fine periodo e confronti 1M/3M/6M/1A: i due valori coincidono **a qualsiasi ora**. Test di regressione: alle 00:30 locali (CEST) l'ultimo punto si ferma al giorno UTC e non conta i movimenti del nuovo giorno locale; anchor dei test in UTC per determinismo su ogni timezone. `tsc` pulito, build OK, **376 test verdi**. `(pending)`
- **[Claude]** **Andamento patrimonio: investimenti al netto degli interessi.** Nella schermata `/wealth-history` la serie *Investimenti* (e quindi il *Patrimonio totale*) ora conta **solo il capitale versato netto** (versamenti − prelievi + saldo iniziale, floor a 0 per categoria): il **controvalore manuale (`currentValue`) non viene più usato come snapshot finale** — la plusvalenza/minusvalenza latente resta fuori dal grafico e il totale di oggi **coincide col Patrimonio netto della dashboard**. Il P/L col controvalore resta visibile nella schermata Investimenti, dove appartiene. Sparisce anche il "gradino" artificiale sull'ultimo punto. Test aggiornati (il currentValue viene ignorato esplicitamente). `tsc` pulito, build OK, **375 test verdi**. `(pending)`
- **[Claude]** **"Andamento patrimonio" + equivalenti serie + avviso novità all'ingresso.** **(1) Nuova schermata `/wealth-history`** (freccia ">" sulla card "Patrimonio netto" della dashboard; il click su "Investito" continua ad aprire gli investimenti): **tre serie STOCK nel tempo** — *Patrimonio totale = liquidità + investimenti* (**investimenti sempre inclusi di default**, indipendentemente dalla preferenza `includeInvestments`), *Liquidità*, *Investimenti*. Motore puro `wealthAnalytics.ts` (stessa matematica per-transazione di `useTransactions`): ricostruzione dai movimenti (entrata ↑ entrambi, uscita `ownShare` ↓ entrambi, **trasferimenti neutri**, versamento investimento sposta liquidità→investimenti **senza toccare il totale**, prelievo speculare, floor a 0 per categoria), esclusi projected/futuri, forward-fill per costruzione, **`currentValue` come snapshot finale** (storico su capitale versato); `buildWealthHistory` / `buildWealthPeriodSummary` / `buildWealthComparisons` (1M/3M/6M/1A) con `deltaPct = (fine−inizio)/|inizio|·100`, `null` se inizio ≈ 0 (UI "—"); bucket per periodo (Mese giornaliero, 3M settimanale, 6M quindicinale, 1A/Tutto mensile). UI stile Sunny: hero (totale, delta, mini-righe liquidità/investimenti), **grafico a 3 linee** (`WealthLineChart`: totale protagonista con area, tratteggio al totale iniziale, tooltip touch/mouse con data+3 valori, negativi ok, empty state), KPI 1M/3M/6M/1A, variazione periodo per metrica, statistiche (max/min/media/miglior/peggior giorno), componenti, "Cosa ha mosso il patrimonio" (barre, trasferimenti = non incidono), **nota interpretativa deterministica** (`buildWealthNote`, niente AI). **(2) Equivalenti serie** centralizzati in `seriesDisplay.ts` (`formatSeriesPrimaryAmount`/`formatSeriesSecondaryAmount`/`buildSeriesEquivalents` su `monthlyEquivalent`): riga secondaria nelle row — annuale→"≈ X € / mese", giornaliero/settimanale→mensile, mensile→"≈ X € / anno"; **rate**: "7 / 24 rate pagate" (posizione nel piano, mai equivalenti); la lista risolve la freq delle istanze dal template. `SeriesDetailSheet`: nuova sezione **"Equivalenti"** per frequenza (annuale: Mensile+Giornaliero; giornaliero: Giornaliero+Mensile+Annuale; settimanale: Settimanale+Mensile+Annuale; mensile: Mensile+Annuale); card rate invariata. **(3) Avviso "Novità in Sunny"** all'ingresso (`ReleaseNotice` + `releaseNoticeStorage`, id `wealth-series-update-2026-07`): una volta per utente (localStorage per-utente), solo autenticati e a caricamento finito; bottom sheet mobile / centrata desktop; CTA "Scopri il patrimonio" → `/wealth-history`, "Ho capito" → chiude; riusabile cambiando id/testi. **Nessuna nuova collection, nessun valore derivato persistito, zero scritture**; ricorrenti/projected/seriesId intatti. `tsc` pulito, build OK, **375 test verdi** (46 nuovi: 27 wealth + 19 serie). `(pending)`
- **[Claude]** **Condivise ricorrenti: anche lo storno si ripete (serie in lockstep).** Creando una spesa **condivisa ricorrente**, il trasferimento di storno veniva creato **solo per il primo evento**: le occorrenze dei mesi successivi nascevano senza il loro storno. Ora, quando la spesa è una serie, **ogni storno diventa una serie a sé** (stessa regola/frequenza/fine, `seriesId` proprio, **stesso `groupId`**) che avanza **in lockstep** con la spesa: ogni mese materializza spesa netta **+** storno con stessa data. Il `groupId` torna a **propagarsi alle occorrenze** (revert degli strip in `expandRecurringOnCreate`/`catchUpRecurring`/`projectOccurrences`/CF): la protezione contro la contaminazione tra mesi resta garantita dalla **guardia stessa-data** in `App.groupTransfers` (PR #92) — modificare la spesa del mese N piega solo lo storno del mese N. Test aggiornati alla nuova semantica + test di coppia in lockstep (expand e catch-up). `tsc` client+functions puliti, build OK, **329 test verdi**. ⚠️ tocca functions → deploy da CI al merge. `(pending)`
- **[Claude]** **Serie intelligenti: ricorrente / abbonamento / a rate — dettaglio serie, badge dedicati, filtro movimenti.** Estensione del sistema `recurring`/`seriesId` esistente, **nessuna nuova collection**. **Dati**: `SeriesKind` + `SeriesMeta` (`kind`, `subscription{label?}`, `installment{totalAmount,totalInstallments,firstDate}`) su `Transaction.seriesMeta`; le serie vecchie senza meta = `recurring`; **nessun valore derivato salvato** (pagato/residuo/equivalenti sempre runtime). **Recurrence**: `monthlyEquivalent(amount,freq)` (stessi moltiplicatori del fixed-cost-load), `nthOccurrenceDate(start,freq,n)` (via `addPeriod`), `buildSeriesSummary(all,anchor,today)` → kind, nextDate, paidCount/paidAmount/paidThisYear, equivalenti (abbonamento), totale/residuo/progress (rate); `dissolveSeries` ora spoglia anche `seriesMeta`. **UI**: nuova `SeriesDetailSheet` (bottom sheet stile Sunny, maschera+finestra interna) con header (icona categoria + badge Ricorrente/Abbonamento/A rate), viste per kind (abbonamento: equivalente mensile/annuale, prossimo pagamento, pagato anno/totale; rate: progress bar X/N, totale piano, pagato, residuo, prossima rata; ricorrente: prossima occorrenza, totale registrato, ultime occorrenze) e CTA (Vedi movimenti serie → `?series=`, Modifica serie via `findTemplate`, Modifica occorrenza solo per occorrenze reali, Chiudi). `openEdit` in App apre la sheet per qualunque membro di serie (sostituisce `SeriesEditChoiceSheet`). **Lista**: filtro `?series=<id>` con pill "Serie: nome" rimovibile, compatibile coi filtri esistenti. **Row**: badge 💳 Abbonamento / 🧾 Rata / 🔁 Ricorrente. **Modal**: blocco "Serie" (Nessuna/Ricorrente/Abbonamento/A rate); abbonamento con preview equivalenti; rate con totale piano+numero rate+prima rata → `amount = totale/rate`, `until = nthOccurrenceDate(...)`, preview rata e ultima rata; le vecchie ricorrenze si aprono come `recurring`; materializzazione invariata (`expandRecurringOnCreate` propaga `seriesMeta` via spread, client+CF senza modifiche). **Rules**: `validTxn` valida `seriesMeta` opzionale (kind enum, installment con numeri >0 e data valida), niente hasOnly rigido; + test emulatore. `tsc` client+functions puliti, build OK, **329 test verdi** (10 nuovi). `(pending)`
- **[Claude]** **Fix condivise ricorrenti: le occorrenze non ereditano più il `groupId` degli storni originali.** Una spesa **condivisa** (storni + spesa netta con `groupId`) resa **ricorrente** contaminava tutta la serie: ogni occorrenza materializzata copiava il `groupId` del template, quindi aprendo un mese qualsiasi il modal mostrava l'importo gonfiato con gli **storni del primo evento** e — peggio — salvando li **cancellava e ricreava alla data sbagliata** (`replaceGroup`). Fix su tutti i punti di materializzazione: **(1)** `expandRecurringOnCreate` — il `groupId` resta solo sull'istanza con `date === base.date` (quella i cui storni esistono davvero); istanze successive e template avanzato senza gruppo. **(2)** `catchUpRecurring` — regola data-driven: l'istanza tiene il `groupId` solo se esistono **fratelli di gruppo alla stessa data** (copre anche template legacy con groupId stantio). **(3)** CF `processRecurringTransactions` — `groupId` sempre escluso dalla copia dell'istanza. **(4)** `projectOccurrences` — righe virtuali senza `groupId`. **(5)** Guardia in `App.tsx` per i dati **già contaminati**: per i membri di serie, i `groupTransfers` contano solo i fratelli con la **stessa data** → le vecchie istanze sporche diventano innocue senza migrazione. `tsc` client+functions puliti, build OK, **322 test verdi** (3 nuovi). ⚠️ tocca functions → deploy da CI al merge. `(pending)`

## 2026-07-08

- **[Claude]** **Sheet come "maschera fissa su finestra scorrevole interna" — niente scroll-chaining, in tutta la PWA.** Le card contestuali non devono muoversi tirando quando lo scroll interno è a fine corsa (né rimbalzare né far scorrere la pagina dietro). **(1) `TransactionModal`** ristrutturata: la card è ora `overflow-hidden flex flex-col` (maschera che non scorre mai) con header e footer-CTA **fissi** come figli flex e i campi del form in una **finestra interna** `flex-1 min-h-0 overflow-y-auto overscroll-contain`; via gli sticky. **(2) `investments/SheetShell`** stessa ristrutturazione (header fisso + contenuto in finestra interna) — copre Versa/Disinvesti/Controvalore. **(3) `overscroll-contain` aggiunto a tutti gli scroller interni** delle altre sheet: OptionSheet, ImportModal (incl. le 3 liste interne), FeedbackSheet, BudgetEditSheet, InsightDetailSheet, EditDefSheet. `AccountDetailSheet`/`CategoryDetailSheet` erano già a posto (pattern di riferimento). Solo struttura/classi, nessuna logica. `tsc` pulito, build OK, **319 test verdi**. `(pending)`
- **[Claude]** **Card transazione: pulsanti d'azione fissi in basso, form che scorre sotto.** In `TransactionModal` i CTA — "Aggiungi {tipo}" / "Salva modifiche" e "Salva e aggiungi un'altra" — sono ora in un **footer `sticky bottom-0`** dentro la card (speculare all'header sticky in alto): full-bleed con margini negativi, stesso sfondo sfumato `--modal-hdr-bg` + `backdrop-blur-xl`, restano sempre raggiungibili mentre il resto del form scorre sotto. "Elimina transazione/serie" resta nel flusso scorrevole (azione distruttiva, meglio non a portata di pollice fisso). Il footer resta dentro il `<form>` così `type="submit"` continua a funzionare. Solo struttura/classi, nessuna logica. `tsc` pulito, build OK, **319 test verdi**. `(pending)`

## 2026-07-03

- **[Claude]** **Mobile: la card della transazione (+) si apre a tutto schermo, restando una card.** Su telefono (sotto `sm`) `TransactionModal` ora riempie lo schermo: `h-full max-w-none` con un margine di 12px + safe-area iOS (`max(0.75rem, env(safe-area-inset-top/bottom))`) attorno, così il backdrop resta visibile ai bordi e i `rounded-3xl` la fanno leggere ancora come **card**, non come una pagina nativa. Da `sm` in su invariata (sheet compatta centrata, `max-w-lg`/`max-h-[88vh]`). Vale sia per la nuova transazione (+) sia per la modifica (stessa modale). Solo classi CSS, nessuna logica. `tsc` pulito, build OK, **319 test verdi**. `(pending)`

## 2026-07-02

- **[Claude]** **Revert completo del motion design (PR #87).** Su feedback dell'utente ("non funzionano bene"), ripristinato lo stato pre-motion: rimossi `shared/motion.ts` e `useDelayedUnmount`, tornate le chiusure immediate delle sheet, via la page transition, i keyframes/easing ricalibrati, il press feedback su nav/righe e il blocco `prefers-reduced-motion`. L'app è identica a prima della PR #87. Un eventuale secondo tentativo andrà fatto in modo più mirato e testato su dispositivo reale. `(pending)`

## 2026-06-29

- **[Claude]** **Forecast V4: eliminata la seconda fonte di "doppio" sulle spese fisse (residuo statistico non più sommato).** Il fix precedente (PR #79) impediva al **budget** di sommarsi sopra una spesa fissa pianificata, ma restava un secondo doppio: se lo storico di una categoria **fissa** (es. "Finanziamento auto") è inserito come **spese singole non ricorrenti** (senza `seriesId`), l'estimatore del **residuo statistico** lo leggeva come spesa "variabile" e prevedeva ~€300 **in più** sopra i €300 già pianificati → €600. Ora, per le categorie fisse (`isFixedCategory`), quando esiste già un importo **impegnato nel mese** (speso/pianificato/ricorrente/stagionale > 0) il **residuo è azzerato** (la spesa è deterministica: una singola occorrenza). Senza nulla di impegnato il residuo resta (una spesa fissa non ancora pagata continua a essere prevista dallo storico), e il budget resta un **pavimento**. Le categorie **variabili** sono invariate. Test di regressione con controllo su categoria variabile. `tsc` pulito, build OK, **319 test verdi**. `(pending)`

## 2026-06-28

- **[Claude]** **Investimenti: versando si aggiorna il controvalore (somma il capitale versato).** In `/investments`, quando si **Versa** in una categoria che ha già un **controvalore** impostato (valore di mercato manuale, `CategoryDef.currentValue`), questo viene ora **sommato al netto investito** del versamento (`nuovo = controvalore + importo`), così riflette subito il capitale fresco e non resta "da aggiornare" — simmetrico al disinvestimento, che già aggiornava `currentValue`. Se la categoria NON ha un controvalore impostato resta invariato (il display usa già il versato come fallback). Nuovo helper puro `investmentValueDeltas()` (netto per categoria dai soli `type==='investment'`, gestisce i ricorrenti espansi, ignora la commissione) + 4 test; wiring nell'`onSave` del deposit sheet in `InvestmentsScreen` (riusa `getCat`/`saveCurrentValue`). Nessuna nuova lettura/collection Firestore. `tsc` pulito, build OK, **318 test verdi**. `(pending)`
- **[Claude]** **Recap mensile (5/5) — push mensile riciclata + deep-link a `/recap/:ym` (deploy = gate umano).** `functions/src/index.ts`: `sendMonthlySummary` ri-orario `0 9 1 * *` → **`0 10 1 * *`** (Europe/Rome), stesso gate `usersWithReminder('monthly')` e stesso calcolo prev-month (nessuna lettura/Cloud Function aggiunta). Testo → CTA recap ("📊 Il tuo riepilogo di {mese} è pronto" + saldo). `sendToUser` ha un nuovo param opzionale `path` che compone `${APP_LINK}{path}?notif=1` (default invariato); qui `recap/${ym}`. SW invariato (FCM apre `fcmOptions.link`); hosting riscrive `**`→index. Build functions OK. **`firebase deploy` NON eseguito**: il deploy delle functions resta gate umano (parte da CI al merge su main). `(pending)`
- **[Claude]** **Recap mensile (4/5) — avviso in-app del nuovo mese (localStorage, zero costo).** `RecapPrompt` montato nello shell autenticato (post-onboarding): al primo accesso in un nuovo mese, se `localStorage['sunny:recapPromptSeen'] != currentYM` e il mese precedente ha ≥1 movimento, mostra un modale ("Vai a vederlo" → `/recap/{prevYM}` · "Più tardi"); entrambe le azioni scrivono `currentYM` (max 1×/mese/device). Nessuna scrittura Firestore. `(pending)`
- **[Claude]** **Recap mensile (3/5) — rotta `/recap/:ym` + archivio "Riepiloghi mensili" nel Piano.** `App.tsx`: nuova rotta deep-linkable (stile `/category-spending`) che passa `tx.transactions`; apribile via URL diretto per il deep-link della push. `BudgetScreenV2`: card che elenca `listRecapMonths(transactions)` (mese + risparmio con segno) → `navigate('/recap/{ym}')`. Per tutti, nessun gate admin. `(pending)`
- **[Claude]** **Recap mensile (2/5) — `MonthlyRecapScreen` + CSS stampa a sfondo chiaro + tabella movimenti.** Schermata (tema scuro a video) con verdetto, KPI doppio-delta (colore = segno semantico, • = fuori dal solito), traiettoria SVG del tasso di risparmio (refLine media, mese corrente evidenziato), driver "cosa è cambiato", narrativa Sunny e tabella movimenti (Data·Tipologia·Categoria·Nota·Importo·Conto). Pulsante "Esporta / Stampa" → `window.print()`. Nuovo blocco `@media print` (`index.css`): A4, **forza la palette chiara su bianco** a prescindere dal tema, nasconde la chrome (`.glass-nav/.glass-header/.no-print`), libera il recap dai container a altezza fissa così pagina su più fogli, `page-break-inside:avoid` sulle card e intestazione tabella ripetuta (`thead { display: table-header-group }`). Nessuna libreria PDF (solo `window.print()`; caveat iOS PWA annotato). `(pending)`
- **[Claude]** **Recap mensile (1/5) — logica pura `monthlyRecap.ts` (+ test, costo Firebase zero).** Modulo puro calcolato **solo dalle transazioni già in memoria** (nessuna nuova collection/lettura). `buildMonthlyRecap()` riusa `insightsEngine.monthStats`/`recentMonths` (ora esportate) come **unica fonte** dei totali (income/uscite netto `shared`/investito/risparmio/tasso) — mai reimplementati. Produce KPI doppio-delta (vs mese prec. e vs "il tuo solito" a 6 mesi, con segni semantici e flag fuori-dal-solito via `mad()`), traiettoria 12 mesi del tasso di risparmio, driver per categoria, verdetto deterministico, streak, narrativa via `buildRuleBasedDigest` (no Gemini) ed elenco movimenti. `listRecapMonths()` per l'archivio in Piano. 8 test verdi. `(pending)`

## 2026-06-27

- **[Claude]** **"Andamento 12 mesi": ripristinato lo stacking uscite + investimenti quando entrambe le serie sono visibili.** I toggle restano indipendenti, ma quando **Uscite e Investimenti** sono entrambe attive, gli **investimenti tornano impilati sopra le spese** (linea oro = uscite + investito = totale uscente); se ne è visibile una sola, viene disegnata dalla baseline. Le Entrate restano sempre una linea propria. `tsc` pulito, build OK, **306 test verdi**. `(pending)`

## 2026-06-25

- **[Claude]** **Dashboard desktop: espandere "Saldo per conto" non sposta più "Investimenti per categoria".** Le card analitiche passano da una griglia 2 colonne (che accoppiava le altezze di riga) a **due colonne indipendenti** su desktop — sinistra: Spese + Investimenti, destra: Saldo — così espandere i conti allunga solo la colonna destra. Su mobile l'ordine resta invariato (Spese → Saldo → Investimenti) tramite `display:contents` + `order-*`. Solo layout, nessuna logica toccata. `tsc` pulito, build OK, **306 test verdi**. `(pending)`
- **[Claude]** **Grafico "Andamento 12 mesi": legenda cliccabile per filtrare le serie + etichetta "Investimenti".** Le tre voci della legenda (Entrate / Uscite / Investimenti) sono ora **toggle indipendenti**: si attivano/disattivano singolarmente e le serie sono disegnate dalla baseline (niente più stacking uscite+investito, così nascondere una non sposta le altre). Etichetta "Investito" → **"Investimenti"**. `tsc` pulito, build OK, **306 test verdi**. `(pending)`
- **[Claude]** **Serie ricorrenti: disattivare "ricorrente" scioglie la serie, data serie modificabile, e fix drift di un giorno nelle proiezioni a lungo termine.** Tre cose. **(1) Disattiva ricorrente = sciogli la serie:** modificando l'intera serie e togliendo "ricorrente", al salvataggio il template diventa un **movimento normale** (niente regola, niente `seriesId`), le occorrenze **future** vengono **cancellate** ("le cose dopo") e quelle **passate già registrate** vengono **svincolate** (perdono `seriesId`/`recurring` → movimenti normali, **senza badge 🔁**, modificabili come singoli). Nuovo helper puro `dissolveSeries`. **(2) Data serie modificabile:** in modifica serie il campo data non è più di sola lettura — si può cambiare la data (le occorrenze future ripartono da lì); rimosso `SeriesStartField`. **(3) Fix drift −1 giorno:** `addPeriod` (client, `shared/recurrence.ts`) faceva il calcolo parse-UTC + mutator **locali** + `toISOString()`: attraversando un confine di **ora legale** le proiezioni a lungo termine slittavano indietro di un giorno (una serie al 10 mostrava il 9 dal 2027). Ora tutto in **UTC** (`Date.UTC` + `setUTC*`). La Cloud Function usava già UTC, quindi le "fatte" materializzate erano corrette: ora client e server coincidono. `tsc` pulito, build OK, **306 test verdi** (5 nuovi: `addPeriod` no-drift, `dissolveSeries`). `(pending)`
- **[Claude]** **"Spese per conto" (dashboard) rispetta l'opzione "Conta gli investimenti nelle uscite".** La card AccountsCard in modalità *spese* ora applica la **stessa logica di visibilità** già aggiunta a Uscite/Movimenti: con l'opzione attiva, il totale spese di **ogni conto** include l'investito netto uscito da quel conto (versamenti +, prelievi −, direction-aware), con la **ⓘ** premibile che mostra la ripartizione **Spese / Investimenti** per conto. Opzione **off** → solo spese, come prima. `currentMonthInvestByAccount` calcolato in `DashboardV2` (stesso filtro `!isPending`/mese-corrente delle spese) e passato a `AccountsCard`. `tsc` pulito, build OK, **301 test verdi**. `(pending)`
- **[Claude]** **Forecast V4: niente più budget sommato sopra alle spese fisse già pianificate.** Bug: una categoria **fissa** (es. "Finanziamento auto") con un movimento pianificato una tantum di 300 € **e** un budget di 300 € mostrava **Previsto 600 €** — V4 sommava il segnale-budget *sopra* l'importo già pianificato (doppio conteggio). Fix: nuova `isFixedCategory` (`forecastV4Common`) per le categorie **fisse/committed** (finanziamento, abbonamenti, assicurazione, rata, mutuo, leasing, canone, università, affitto, bollo). In `computeForecastV4` il **segnale-budget additivo** (gap × affidabilità) ora si applica **solo alle categorie variabili**; per quelle **fisse** il budget è un **pavimento** (`previsto = max(forecastBeforeBudget, budget)`), così il **Previsto coincide col Programmato** e non si somma mai (planned 300 + budget 300 → 300, non 600). Le variabili (Acquisti, Pieno mezzi…) restano invariate. `tsc` pulito, build OK, **301 test verdi** (2 nuovi). `(pending)`
- **[Claude]** **Impostazioni investimenti: opzione "Conta gli investimenti nelle uscite" + dettaglio ⓘ.** Nuovo toggle (sezione Investimenti, default **off** → comportamento invariato) `countInvestmentsInExpenses` in `settings` provider + `SettingsScreen`. Quando **on**, il totale delle **Uscite** include gli investimenti: nella card "Uscite" della dashboard (uiV2) il valore diventa spese + investito netto e la card "Risparmio" resta `Entrate − Uscite` (le tre card tornano coerenti); nei **subtotali dei raggruppamenti** in Movimenti gli investimenti contano come uscita (direction-aware: i versamenti sottraggono, i prelievi sommano). Dove il totale include gli investimenti compare una **ⓘ** premibile (`shared/components/OutflowInfo`) che mostra la ripartizione **Spese / Investimenti**. Off = tutto come prima (investimenti fuori dai totali uscite). `tsc` pulito, build OK, **299 test verdi**. `(pending)`
- **[Claude]** **Piano: navigazione tra i mesi + il motore di forecast V4 diventa ufficiale ovunque (V3 dormiente).** Due cose insieme. **(A) Navigazione mesi nel tab Piano:** `BudgetScreenV2` ha ora frecce ‹ › (+ "Oggi") per muoversi tra i mesi — indietro fino al primo mese con dati, avanti fino a +12 mesi. Ogni slice (entrate/uscite/investimenti realizzati, programmato, budget per categoria, previsione, stato) è ricalcolata per il **mese selezionato**: il **corrente** resta identico a prima (stesse regole `isPending`), i **passati** mostrano il **consuntivo** (budget di quel mese vs speso reale, etichetta "Consuntivo"), i **futuri** permettono di **fissare i budget** e ne mostrano la **proiezione** (alle spese ricorrenti virtuali del mese futuro si aggiungono le occorrenze proiettate, così le previsioni includono gli impegni). `useBudget` ha nuovi writer per **mese arbitrario** (`setCategoryBudgetFor`/`…Income…`/`…Investment…`/`setSavingsTargetFor`/`resetAllFor`/`confirmMonth`/`copyPrevInto`): il mese corrente mantiene il mirror legacy `meta/budget`, gli **altri mesi scrivono solo `budgetHistory/{YYYY-MM}`** (navigare/modificare un mese non disturba il corrente). I mesi passati sono **modificabili** (riscrivono lo storico). **(B) V4 ufficiale ovunque:** i call-site di produzione passano da V3 a V4 cambiando solo l'import — `BudgetScreenV2`, `BudgetScreen` (legacy) e `insightsEngine` usano i wrapper drop-in `forecastSavingsV4`/`forecastByCategoryV4` (stesse firme/shape di quelli V3). V4 prevede di suo **solo le spese**: per il numero "risparmio previsto" il nuovo `forecastFlowsV4` aggiunge la previsione di **entrate e investimenti** (realizzato + impegnato + mediana storica) — V4 possiede ora l'intero calcolo. I wrapper chiamano l'engine **senza `user`**, quindi il gate admin di V4 è un no-op: **V4 è il motore di default per tutti**. L'insight admin "categorie imprevedibili" non dipende più dal V3: ora usa un'euristica di **rarità** auto-contenuta (categoria presente in ≤40% degli ultimi 12 mesi). I file V3 restano nel repo ma **non più importati in produzione**; la pagina admin `/forecast-v3` (con confronto V4) resta. Rinominato il campo input degli insight `forecastV3Categories` → `forecastExpenseCategories` ovunque. Nessuna migrazione dati. `tsc` pulito, `npm run build` OK, **299 test verdi** (nuovi test per `forecastFlowsV4`/`forecastCompatV4`). `(pending)`

## 2026-06-24

- **[Claude]** **Ricorrenti/serie: rework non-distruttivo + modifica corretta di serie e singoli + badge 🔁.** Cambio coeso su come si gestiscono movimenti e serie. **(1) Niente cancellazioni automatiche dal codice:** `catchUpRecurring` (client) e `processRecurringTransactions` (CF notturna) non cancellano più alcun template — una serie conclusa viene **avanzata oltre `until`** (il template diventa *scaduto*), mai eliminata; `materializeRecurring` perde il ramo di delete. **(2) Template scaduti tenuti ma nascosti:** `useTransactions` espone `transactions` (template scaduti **filtrati** → liste/totali puliti) **+** `allTransactions` (set completo); `findTemplate` cerca su `allTransactions`, così una **serie conclusa resta apribile/modificabile come serie**. `deleteAll` usa il set raw. **(3) Modifica = update sul posto:** nuovo `replaceInPlace` (`setDoc` sullo **stesso id**, niente delete+recreate) usato da `handleSave` per le modifiche a documento singolo (movimento normale, istanza singola, template di serie) — così **una transazione già inserita non viene mai cancellata** dalla modifica; solo le ristrutturazioni di gruppo (spesa condivisa/commissione → più documenti) restano su `replaceGroup`. **(4) Date corrette in modifica:** aprendo **la serie** la modale mostra **Inizio serie** (prima occorrenza, sola lettura) + frequenza + fine, e salva mantenendo il puntatore interno alla prossima occorrenza (niente riga fantasma); **"modifica singolo"** apre quell'occorrenza con la **sua** data; un movimento normale apre con **la sua** data. **(5) Badge 🔁** su **ogni occorrenza registrata** di una serie (deriva da `seriesId` → compare anche sulle **vecchie**), non solo sui template. Modello dati invariato (nessuna migrazione). `recurrence.test.ts` aggiornato al catch-up non-distruttivo. `tsc` client+functions pulito, `npm run build` OK, **287 test verdi**. **PITR** (recupero 7 giorni) attivato lato Firebase come rete di sicurezza. `(pending)`

## 2026-06-23

- **[Claude]** **Fix ricorrenti: le occorrenze passate ("fatte") non venivano materializzate se UNA voce falliva.** Il catch-up lato client (`useTransactions.materializeRecurring`, lanciato all'avvio da `App`) scriveva creazioni + avanzamenti template + cancellazioni in **un unico batch atomico** con `batch.commit()` **non awaited e senza catch**. Conseguenza: era **tutto-o-niente** — bastava **un** template malformato (es. doc legacy che le regole irrobustite ora rifiutano) **oppure** un arretrato > 500 scritture (limite batch Firestore) per far fallire silenziosamente l'intero batch → **nessuna** occorrenza materializzata per **nessuna** serie, lasciando ogni ricorrente bloccata come "Programmato"/"previsto" senza "fatto" (a prescindere dal tipo). Riscritto con **scritture indipendenti** (`addDoc`/`updateDoc`/`deleteDoc` + `Promise.allSettled`, errori loggati per-voce): una riga problematica perde solo sé stessa. Le **creazioni vengono prima** e si attende il loro esito; solo dopo si avanzano i template (se una creazione fallisce, il prossimo catch-up riprova — `catchUpRecurring` deduplica le occorrenze già materializzate, niente doppioni). **Self-heal**: al primo riavvio dell'app dopo il deploy, le "fatte" mancanti vengono ricreate automaticamente. La Cloud Function notturna `processRecurringTransactions` è già resiliente per-serie (un batch per template, Admin SDK che bypassa le regole) e resta invariata. `tsc` pulito, build OK, **287 test verdi**. Nessun bump `APP_VERSION`. `(pending)`
- **[Claude]** **Revert: gli investimenti tornano visibili nel tab Movimenti.** Annullato il filtro `type !== 'investment'` introdotto nella Parte B (PR #69): la rotta `/transactions` ripassa di nuovo `tx.transactions` e `projected` interi a `<TransactionList>` (rimosse le derivazioni `movimentiTransactions`/`movimentiProjected`). Motivo: il filtro nascondeva **anche le occorrenze ricorrenti già registrate** (es. versamenti/PAC ricorrenti — le "fatte") oltre alle previste, e — cliccando una riga — era l'unico punto da cui si potevano **modificare/eliminare** gli investimenti esistenti. Ora nei Movimenti si rivedono storico + previsti degli investimenti, ripristinata la pill "Investimento" (deriva dai tipi presenti) e l'edit/delete via riga. Invariati gli altri comportamenti (creazione nuovi investimenti resta in `/investments`; il tipo "Investimento" resta fuori dal selettore della modale per le nuove transazioni). `tsc` pulito, build OK, **287 test verdi**. Nessun bump `APP_VERSION`. `(pending)`
- **[Claude]** **Metriche: voce di menù nelle Impostazioni per la schermata admin `/metrics`.** Aggiunta una `Row` **"Metriche (admin)"** (📈, `DAU/WAU/MAU e engagement` → `navigate('/metrics')`) in `SettingsScreen`, sia nel menu uiV2 (sezione "Avanzate", accanto a "Previsione V4 (admin)") sia nella griglia legacy. Gated da `isAdminUser(user)`: è **accesso a DATI admin** (la collection `metrics/*` è admin-readable nelle rules), uso legittimo dell'identità admin — non un gate di visibilità feature. Prima la rotta `/metrics` era raggiungibile solo digitando l'URL. `tsc` pulito, build OK, **287 test verdi**. Nessun bump `APP_VERSION`. `(pending)`
- **[Claude]** **Metriche self-hosted — CI: aggiunte le nuove function alla deploy allowlist.** `deploy-functions.yml` usa `deploy --only functions:<nomi espliciti>`: aggiunti `functions:testMetricsRollup` (step HTTP) e `functions:rollupMetrics` (step scheduled/trigger), altrimenti — come avverte il commento del workflow — non sarebbero MAI state deployate al merge su main. Deploy invariato per il resto; gli step restano gated `if: github.event_name == 'push'` (le PR non deployano). `(pending)`
- **[Claude]** **Metriche self-hosted su Firestore — COMMIT 4: `notif_open` + schermata admin `/metrics`.** **notif_open**: il link delle push ora porta `?notif=1` (`sendToUser` in `functions/src/index.ts`, sia `webpush.fcmOptions.link` sia `data.link`) — **nessun handler `notificationclick` custom nel service worker** (entrerebbe in conflitto con quello di default di FCM, che già apre `fcmOptions.link`; SW invariato, nessuna scrittura Firestore nel SW). Lato client (`App.tsx`), al load se è presente `?notif=1` si logga `notif_open` **una volta** e si rimuove il param dall'URL (`history.replaceState`). **Schermata admin**: nuova `features/metrics/MetricsScreen.tsx` (legge la collection top-level `metrics/*`, ultimi 60 giorni, ordinati per data) con KPI (DAU/WAU/MAU/stickiness/nuovi/totali), grafico a linee DAU/WAU/MAU nel tempo e barre lettori/adozione dell'ultimo giorno. Rotta `/metrics` in `App.tsx` gated da `isAdminUser(user)` (→ `Navigate` a `/` per i non-admin): **accesso a DATI admin**, non un gate di visibilità feature (uso legittimo dell'identità admin, coerente con l'header di `featureFlags.ts`); nessuna voce di nav aggiunta. `tsc` client+functions pulito, `npm run build` OK, **287 test verdi**. Nessun bump `APP_VERSION`, feature invisibile agli utenti normali. `(pending)`
- **[Claude]** **Metriche self-hosted su Firestore — COMMIT 3: Cloud Function di rollup + retention.** Nuovo `rollupMetrics` (`onSchedule '15 0 * * *'`, `Europe/Rome`, `europe-west1`, 540s/512MiB): aggrega il **giorno completo precedente** (Rome) e scrive `metrics/{YYYY-MM-DD}` `{ date, dau, wau, mau, stickiness, newUsers, totalUsers, readers{insights_view,insight_open,notif_open}, adoption{tx_add,forecast_view,aicoach_open}, generatedAt }`. DAU/WAU/MAU/stickiness da `meta/activity.activeDays` (scan singolo di `/users` via `listDocuments()`); readers/adoption = **utenti distinti** con quell'evento nel giorno target (query `events` per-utente sul range ms del giorno Rome, con helper DST-safe `romeDayStartMs`/`tzOffsetMs` — verificato su giorni di cambio ora: span 23h/25h); newUsers/totalUsers via `admin.auth().listUsers(1000)` su `creationTime` (1 pagina a scala beta, **TODO paginazione**). **Retention**: nello stesso fn elimina gli `events` con `ts` più vecchio di 90 giorni (batch ≤450, per-utente). Fallimenti per-utente loggati e saltati (mai abortire il run). Nuovo endpoint admin `testMetricsRollup` (`onRequest`, `verifyBearer` + UID admin, CORS ristretto, opzionale `?day=YYYY-MM-DD`) per trigger on-demand. `onUserDeleted`: **già** elimina `meta/activity` + `events` via `recursiveDelete(users/{uid})` (commento reso esplicito; nessuna modifica funzionale). Allowlist eventi identica a `metrics.ts` e `firestore.rules`. `cd functions && npx tsc --noEmit` **pulito**, `npm run build` OK. ⚠️ **GATE pre-deploy**: la verifica live di `testMetricsRollup` (doc reale in dev) va fatta dopo il deploy via CI; nessun deploy diretto. Nessun bump `APP_VERSION`, feature invisibile agli utenti. `(pending)`
- **[Claude]** **Metriche self-hosted su Firestore — COMMIT 2: regole Firestore + test emulatore.** Aggiunte regole **solo additive** (nessuna regola esistente toccata): nuovo helper `validEvent(d)` = `d.keys().hasOnly(['name','ts']) && d.ts is number && d.name in [allowlist]`; dentro `users/{userId}` nuovo `match /events/{eventId}` (owner: `read` + `create` con `validEvent`; **update/delete negati** ai client — la purge è Admin SDK); top-level `match /metrics/{day}` (`read` solo `isAdmin()`, `write` sempre negato). `meta/activity` è già coperto dalle regole `meta/{doc}` esistenti (nessuna modifica). Allowlist eventi identica a `metrics.ts` (sync). Nuovi test emulatore in `firestore-tests/rules.test.ts`: eventi (owner crea name valido OK + legge; name fuori allowlist negato; campo extra negato; `ts` non numerico negato; altro utente negato; update/delete negati) e metrics (write client negato; admin read OK; non-admin read negato). **Emulatore: 19 test verdi** (12 esistenti senza regressioni + 7 nuovi). Nessun bump `APP_VERSION`, feature invisibile agli utenti. ⚠️ **GATE**: richiede review umana del diff `firestore.rules` prima del merge a main (il merge fa deploy regole via CI). `(pending)`
- **[Claude]** **Metriche self-hosted su Firestore — COMMIT 1: layer di tracking client (invisibile agli utenti).** Nuovo `sunny/src/shared/analytics/metrics.ts` (fire-and-forget, mai blocca il render, mai propaga errori in UI): `recordActivity(uid)` aggiorna `users/{uid}/meta/activity` `{ lastActiveAt, activeDays[] }` (debounced **una volta per sessione** via sessionStorage `sunny_activity_done`; legge→dedup→sort→trim ultimi 35 giorni); `logEvent(uid, name)` scrive `users/{uid}/events/{autoId}` con **SOLO** `{ name, ts }` (guardia allowlist client-side); helper PURI esportati `todayKey(now)`/`pushActiveDay(days, day)`. **Nessun dato finanziario** in eventi/attività (mai importi/descrizioni/categorie/merchant). Allowlist eventi: `app_open`, `insights_view`, `insight_open`, `notif_open`, `tx_add`, `forecast_view`, `aicoach_open`. Call site: `App.tsx` (1×/sessione → `recordActivity` + `app_open`; `tx_add` in `handleSave` solo per i nuovi inserimenti, non per le modifiche); `InsightsScreen`/`InsightsScreenV2` (`insights_view` al mount, `insight_open` all'apertura del dettaglio); `ForecastV3Screen` (`forecast_view` al mount); `AICoachScreen` (`aicoach_open` al mount). `@firebase/analytics` resta non inizializzato (niente GA4). **+6 test** puri (`metrics.test.ts`: todayKey, dedup/trim, allowlist). Doc dati aggiornati (`ARCHITETTURA.md`/`HANDOFF.md`). `tsc` pulito, build OK, **287 test verdi**. Nessun bump `APP_VERSION`, nessun annuncio: feature invisibile agli utenti. `(pending)`
- **[Claude]** **Dashboard: i due grafici a ciambella ("Spese per categoria" e "Investimenti per categoria") ora hanno la stessa dimensione.** La ciambella di `CategoryCard` passa da `size={96}` a `size={132}`, allineandosi a quella di `InvestmentSummaryCard` (entrambe le card stanno nella stessa griglia a 2 colonne, quindi stessa larghezza). Scelto il valore più grande perché il totale al centro della card spese ci sta comodamente, mentre rimpicciolire quella investimenti avrebbe stretto il suo totale (numero più lungo). Modifica condivisa tra dashboard V2 e legacy (stesso componente). `tsc` pulito, build OK, **281 test verdi**. `(pending)`

## 2026-06-22

- **[Claude]** **Rimosso il tipo "Investimento" dal form "Nuova transazione" (per tutti).** Il selettore di tipo della `TransactionModal` non offre più la tab **"Investimento ↗"** per le nuove transazioni: gli investimenti si creano e gestiscono **esclusivamente** dalla schermata `/investments` (sheet deposito/disinvestimento/valore). `availableTypes` ora esclude `'investment'`, **tranne** quando si **modifica** un investimento esistente (così il tipo si visualizza correttamente e l'edit non viene ritipizzato). Rimossa la macchina del "redirect" ormai morta (stato `investRedirect`, blocco messaggio "Gli investimenti si gestiscono dalla schermata Investimenti →", e l'import `useNavigate`/`navigate` non più usato). Nessun impatto sui flussi di `/investments` (usano i loro sheet dedicati, non la modale) né sull'import CSV (scrive le transazioni direttamente). Coerente con la rimozione degli investimenti dal tab Movimenti: ora il tipo `investment` non è creabile da nessuna modale generica. `tsc` pulito, build OK, **281 test verdi**. `(pending)`
- **[Claude]** **Riconoscimento categoria L1+L2 nel form transazione — _admin-only_ (`isAdminUser`).** Solo per l'utente admin il campo descrizione della modale ora usa un riconoscitore a due strati; per i non-admin il comportamento è **identico a prima** (`guessCategory` invariato, bug "gasolio→Casa" incluso, voluto per scope admin-only). Nuovo modulo puro e deterministico (nessuna rete, nessun Gemini) `features/transactions/categoryRecognition.ts`: **L1 — memoria storica** (`buildMerchantIndex` costruisce `merchantKey → (categoryId → peso)` dalle transazioni reali dell'utente, peso = frequenza + leggero bonus di recency relativo allo span del dataset; `normalizeMerchant` rimuove rumore POS/date/numeri/sigle carta + parole di via e città e collassa ai token significativi, es. "PAGAMENTO POS 12/06 ESSELUNGA MILANO" → `esselunga`); **L2 — fallback keyword corretto** (tokenizzazione con confini di parola, niente `includes` grezzo → "barbiere" non è "bar", "gasata" non è "gas"; scoring di **tutte** le categorie candidate, niente first-match-by-order; keyword disaccoppiate dagli id via tag semantici risolti ai candidati **per id di default O per label**, così le categorie custom/rinominate beneficiano). `recognizeCategory` fa il lookup L1 **ristretto ai candidates** (il tipo è già filtrato → nessun leakage cross-type) e, se la storia non è affidabile, passa a L2; ritorna `{categoryId, confidence}|null`. `CATEGORY_KEYWORDS` ora **esportato** da `utils.ts` e riusato (niente duplicazione); `guessCategory` intatto. Nuovo hook `useCategoryRecognizer(user, transactions)` in `App.tsx` (indice memoizzato) iniettato in `TransactionModal` come prop opzionale `recognize`: nell'`onChange` della descrizione, se `recognize` è presente e `!categoryTouched`, applica la categoria solo a `confidence ≥ RECOGNITION_THRESHOLD` (0.5), altrimenti fallback a `guessCategory`. **+13 test** (`categoryRecognition.test.ts`: regressione benzina/gasolio→trasporti, confini di parola, L1 storico esselunga, L1 ristretto ai candidates, normalizzazione, soglia). `tsc` pulito, build OK, **281 test verdi**. Nessun bump `APP_VERSION` (feature interna admin-only). `(pending)`
- **[Claude]** **Investimenti fuori dal tab Movimenti (per tutti) — filtro di sola visualizzazione.** La rotta `/transactions` ora filtra `type !== 'investment'` sia su `transactions` sia su `projected` passati a `<TransactionList>` (due valori derivati memoizzati in cima al render di `Main`, con commento esplicativo) — **nessuna scrittura, nessuna migrazione, modello dati / `Transaction` / `TYPE_ORDER` / `TYPE_META` / `TransactionModal` / `useTransactions` invariati**. In Movimenti spariscono le righe investment (reali e "Programmato"), la pill **"Investimento"** sparisce da sola (`usedTypes` in `TransactionList` deriva dai tipi presenti — **verificato, non modificato**) e la ricerca non restituisce più investimenti. I deep link `?account=<id>` / `?cat=<id>` su un conto/categoria d'investimento mostrano (come atteso) zero righe. Investimenti **invariati altrove**: dashboard (FlowBar/TrendChart), forecast, budget, patrimonio netto, `/investments`. ⚠️ **SEGNALAZIONE (non risolta qui, fuori scope):** rimuovendo gli investimenti da Movimenti viene a mancare **l'unico punto UI** da cui si potevano **modificare/eliminare** le singole transazioni d'investimento esistenti (l'`openEdit`→`TransactionModal` era cablato solo sulla `TransactionList` di `/transactions`); la schermata `/investments` elenca le operazioni in sola lettura (`OpRow` senza `onClick`) e i suoi sheet **aggiungono** soltanto (deposito/disinvestimento/valore), non editano né cancellano. Da valutare un entry point di modifica/eliminazione in `/investments`. **APP_VERSION**: questa è user-visible → un patch bump è difendibile, ma **lasciato alla decisione di Luca** (non bumpato in questo changeset). `tsc` pulito, build OK, 281 test verdi. `(pending)`

## 2026-06-20

- **[Claude]** **Fix dashboard: "Spese per categoria" ora coincide con il totale "Uscite" del mese.** La `CategoryCard` di `DashboardV2` ricalcolava le spese per categoria da `p.transactions` filtrando solo per mese/anno, **senza escludere le spese future** (pianificate / ricorrenti non ancora scadute): includeva quindi voci che il totale "Uscite" (`monthlyExpenses`, basato su `!isPending`) escludeva → la somma delle categorie risultava più alta del totale del mese. Aggiunto il filtro `isPending` (stesso di `useTransactions`) al calcolo di `currentMonthCategoryTotals` e `currentMonthExpenseByAccount` in `DashboardV2.tsx`, così categorie e conti combaciano col mese. La schermata di drill-down `/category-spending` (`categoryAnalytics.ts`) e la dashboard legacy erano già corrette. Solo `DashboardV2.tsx` toccato; `tsc` pulito, 268 test verdi.
- **[Claude]** **Hardening backend (Priorità 0/1/2/6) — zero modifiche client.** Nessun file `sunny/src/**` toccato; UI/UX/forecast/import/shortcut client invariati. **`firestore.rules`**: validazione transactions non-breaking (enum `type`, `date` `^\d{4}-\d{2}-\d{2}$`, lunghezze, `amount>0 && <=1e9`, `shared/tfr/direction/recurring` validati a presenza; **niente `hasOnly`** — fase 2); `account` consente `''`; regola **mirata** `users/{uid}/derived/encouraging` owner-only + payload (`items` list ≤50, no `hasOnly` per non rompere il merge della Cloud Function); hardening `feedback` (presenza+enum+lunghezze+`hasOnly`, read solo admin) e `budgetHistory` (numeri≥0, enum status/source); `forecastSnapshots` create-only confermato; `expenseTokens` deny confermato. Tutte le operazioni client esistenti (delete tx, delete meta/settings, ecc.) preservate. **`functions/src/index.ts`**: `onUserDeleted` ora fa **purge completo** via `recursiveDelete(users/{uid})` + cleanup `expenseTokens` e `feedback` dell'utente (timeout 540s/512MiB); **rate-limit atomici** (`runTransaction`) per AI coach (20/g) e token shortcut (30/h); **cap giornaliero `generateDigest` 30/utente**; **timeout 12s** sulle fetch Gemini (`AbortController`); body-size guard (413), cap importi `1e9` e lunghezze; **log sanitizzati** (solo code/message, mai dati finanziari). (La gestione di `GEMINI_API_KEY` resta invariata via `.env` in CI — il passaggio a Secret Manager è stato volutamente escluso da questo step.) **CI**: `deploy-functions.yml` ora `npm ci` (+ `functions/package-lock.json` committato, rimosso dal `.gitignore`), `firebase-tools` pinnato a major `13`, `npm audit --audit-level=high` (oggi 0 high/critical); nuovo `.github/dependabot.yml`; nuovo workflow **`ci-tests.yml`** (solo PR, non deploya) con test client + build functions + **test regole su emulatore Firestore** (`firestore-tests/`). **`firebase.json`**: security headers conservativi (nosniff, frame-DENY, referrer-policy, permissions-policy, HSTS; **niente CSP** — fase separata). Verificato: functions `tsc` build OK, sunny `tsc` + 268 test verdi. **Nessun passo manuale richiesto** (la chiave Gemini continua a essere iniettata via `.env` in CI come prima). Fuori scope/segnalati: CSV-injection (export client), `monthlyStats`/subscriber-index (step separati), staging, Secret Manager (rinviato). `(pending)`

## 2026-06-19

- **[Claude]** **Fix: rendere ricorrente una spesa vecchia ora crea davvero le occorrenze.** Convertendo un **one-off già inserito** (senza `recurring` né `seriesId`) in serie ricorrente, le occorrenze passate (dalla data originale a oggi) vengono **materializzate subito** al salvataggio — prima la modale saltava l'espansione su qualsiasi edit (`finalize = editing ? docs : …`), quindi i mesi mancanti non venivano creati al momento (restavano demandati al catch-up/Cloud Function e sembravano non comparire). Nuovo predicato puro `shouldExpandOnSave(editing, isRecurring)` in `shared/recurrence.ts` (true per nuove transazioni e per la conversione one-off→serie; **false** per edit di serie/istanze già esistenti, così non si duplica) usato in `TransactionModal`. Audit saldo: matematica dei segni coerente tra `useTransactions` e `accountAnalytics`, quadratura per costruzione (nessun bug). Liquidità lasciata invariata su richiesta. **+6 test** (`shouldExpandOnSave` + materializzazione conversione) — 268 verdi, `tsc` pulito, build OK. Nessun bump `APP_VERSION`, nessun avviso. `(pending)`
- **[Claude]** **Rimossi i "Consigli Sunny" dalla schermata Piano (tutti gli utenti).** Eliminata la card `BudgetInsights` ("Consigli Sunny") da `BudgetScreenV2` (live) e dal legacy `BudgetScreen`; rimossi `insights`/`generateBudgetInsights` ormai inutilizzati (`budgetUtils.ts`, con gli helper privati `euro`/`round10ish` e l'interfaccia `BudgetInsightInput`) e cancellato il componente orfano `BudgetInsights.tsx`. Resta tutto il resto del Piano (suggerimento budget, banner stagionale, panoramica, liste). Modifica **silenziosa**: nessun `WhatsNewModal`/avviso push, nessuna voce in `appInfo.ts` VERSIONS, **niente bump di `APP_VERSION`**. 262 test verdi, `tsc` pulito, build OK. `(pending)`
- **[Claude]** **Budget mensile con storico + correzione budget signal/backtest V4 (sezione 17).** Il budget diventa **mensile e storicizzato**, senza rompere la retrocompatibilità. Nuovo modello puro `features/budget/monthlyBudget.ts` (`MonthlyBudget` con `status` draft/confirmed/missing/auto_initialized + `source`, helper `initMonthlyBudget`/`confirmMonthlyBudget`/`applyMonthlyBudgetEdit`/`shouldShowBudgetSetupPrompt`/`prevMonthKey`). Persistenza in `users/{uid}/budgetHistory/{YYYY-MM}` (nuova regola Firestore owner-only) con `meta/budget` mantenuto come **mirror** del mese corrente; `useBudget` ora: auto-inizializza il mese corrente copiando il precedente (o il legacy) come **non confermato**, espone `monthly/monthlyStatus/budgetHistory/confirmCurrentMonth/copyFromPreviousMonth/showBudgetPrompt`, e instrada ogni edit sia sul mirror sia sullo snapshot mensile (→ `draft`). **Banner persistente** `BudgetSetupBanner` (Conferma / Modifica / Ricordamelo +24h) montato in `App` finché il mese non è confermato; `BudgetScreenV2` mostra mese, badge stato e azioni Conferma/Ricopia. **Forecast V4 corretto**: usa `budgetHistory[targetMonth]` (mai il budget corrente in modo retroattivo); aggiunge `budgetStatus`/`budgetSource`/`budgetReliabilitySource`/`budgetReliabilitySampleCount` per categoria + `budgetMonthStatus`/`budgetSource`/`budgetHistoryCoverageRatio`/`budgetSignalValidatable` nei diagnostics; reliability **empirica solo da mesi confermati** (≥3), altrimenti fallback; segnale **dimezzato per categorie planned-like e azzerato per le discrezionali** se il budget non è confermato. **Backtest corretto**: budget signal applicato **solo ai mesi con budgetHistory** (mai il budget corrente sui mesi storici), nuove metriche `sampleCountBudgetMonths`/`sampleCountWithoutBudget`/`budgetHistoryCoverageRatio` + warning "Budget signal non ancora validabile" se <3 mesi confermati. **Export diagnostico** aggiornato (`budget.currentMonth`+`currentBudget` con status/source, `budgetHistory[]`). **+26 test** (monthlyBudget, gating engine/backtest, export) — totale **262 verde**, `tsc` pulito, build OK. Nessun bump `APP_VERSION`. `(pending)`

## 2026-06-18

- **[Claude]** **Forecast Engine V4 (planned/seasonal/budget-aware) — admin-only, dietro feature gate.** Nuovo motore di previsione spese mensili **isolato** da V3 (nessun import da `forecastEngineV3`; condivide solo helper statistici e tipi cancellati a compile-time). Decompone ogni categoria **expense** in `spentToDate + plannedManualRemaining + recurringRemaining + seasonalDetectedRemaining + residualStatisticalRemaining + budgetSignalAdjustment` (income/investment/transfer **esclusi**). Gate centralizzato `src/features/forecast/forecastFeatureGate.ts` (`isForecastV4EnabledForUser` = `role==='admin'` ∨ UID in whitelist ∨ remote flag; `assertForecastV4Access` guardia hard nel motore). Gli utenti non-admin **non vedono toggle/diagnostica/V4 e restano su V3** (`App.tsx` passa `forecastV4Enabled={isForecastV4EnabledForUser(user)}` a `ForecastV3Screen`, che monta `ForecastV4Panel` solo se true). Nuovi moduli in `src/features/forecast/v4/`: `forecastTypesV4`, `forecastV4Common` (similarità 15%/50€, percentile P60, reliability fallback per-categoria, esenzioni stale-decay), `forecastPlannedV4` (planned/recurring + classificatori deterministic-like anti-doppio-conteggio), `forecastSeasonalityV4` (`detectSeasonalExpensesV4`: assicurazioni feb/ago ~875 da 2 occorrenze ≥300€ → mediana, confidence high; 1 occorrenza periodica → medium; niente duplicati se già planned/recurring/spent), `forecastResidualV4` (coda storica **P60** escludendo deterministic-like + stale-decay ×0.5 con esenzioni Finanziamento/Abbonamenti/Assicurazioni/budget/planned), `forecastBudgetSignalV4` (`computeBudgetReliabilityV4` empirica da budgetHistory con fallback + `computeBudgetSignalAdjustmentV4` = gap×reliability, **mai** `max(forecast,budget)`, zero se gap già spiegato ≥75% da planned/seasonal o budget = tetto), `forecastEngineV4`, `forecastBacktestV4` (confronto 4 modelli V3/V3.5-bias/V4/V4+budget su giorni 1·5·10·15·20·25, MAE/WAPE/Bias/RMSE/R² + per giorno/mese/categoria, budgetSignalHelped/Hurt, plannedCoverageRatio; non crasha senza budgetHistory), `forecastDiagnosticsV4` (confronto V4↔V3, pesi componenti, report esportabile). Pannello UI admin `ForecastV4Panel.tsx` (composizione, dettaglio per-categoria con speso/forecast-prima-del-budget/budget/gap/reliability/adjustment/forecast-finale/reasons, toggle segnale budget, warning «pianifica spese >300€», backtest on-demand, export JSON). **Export GDPR** (`dataExport.ts`) ora include opzionalmente `budget` e `budgetHistory` (threaded `App`→`SettingsScreen`), backward-compatible (`schemaVersion` invariato). **+57 unit test** (gate, seasonal, budget signal+reliability, residual P60+stale, engine, backtest, diagnostics, export budget) — totale **246 verde**, `tsc` pulito, build OK. **Nessun bump `APP_VERSION`, nessun annuncio**: feature invisibile ai non-admin. `(pending)`

## 2026-06-17

- **[Claude]** **UI v2.0.0 standard per tutti.** `canUseUiV2` ora ritorna `!!user` (stile `canUseDetailedInvestments`/`canUseForecastV2`), quindi ogni utente loggato riceve `uiV2=true` (App.tsx → SideNav/BottomNav/TransactionList/SettingsScreen + rami Dashboard/Insights/Budget). Rimosse le allow-list ormai morte `UI_V2_UIDS`/`UI_V2_EMAILS` (usate solo da `canUseUiV2`, nessun riferimento pendente in `src/`). Unico file toccato: `src/shared/featureFlags.ts`. **Nessun annuncio al login**: niente `WhatsNewModal`/popup "Novità", nessun `highlight:true`, nessuna voce in `appInfo.ts` VERSIONS e **niente bump di `APP_VERSION`** (così il "già visto" in localStorage non si invalida). `isAdminUser`, `firestore.rules` e i componenti V2 invariati. `(pending)`
- **[Claude]** **Soft-delete silenzioso di categorie e conti (tutti gli utenti).** Eliminare una voce **referenziata** nello storico ora la marca `archived: true` invece di rimuoverla (resta nell'array sorgente → `getCat`/`getAcc` continuano a risolverla per lo storico); una voce **mai usata** viene rimossa come prima. Logica pura testata in `features/settings/softDelete.ts` (`isCategoryUsed`/`isAccountUsed`, `removeCategoryDef`/`removeAccountDef`, `visibleDefs`) con `softDelete.test.ts` (+8 test). Nuovo campo `archived?` su `CategoryDef`/`AccountDef` (`types.ts`). Nuovi `visibleCategories`/`visibleAccounts` memoizzati nel context (`settings.tsx`; `categories`/`accounts` restano la sorgente completa, `getCat`/`getAcc` **invariati**). Le archiviate sono nascoste da: gestione conti/categorie + drag-reorder (`SettingsScreen` — indici sui visibili, salvataggio ricostruisce l'array completo `[...visibiliRiordinate, ...archiviate]`), picker nuova transazione (`TransactionModal` — ma una voce archiviata già referenziata resta selezionabile **in modifica** per non riassegnarla), righe budget (`BudgetScreen`/`BudgetScreenV2`), allocazione investimenti (`InvestmentsScreen` con totali coerenti dai visibili + sheet versa/disinvesti), input forecast/insight (`App` ForecastV2/V3 + `allCategories`/`accounts`, `Dashboard`/`DashboardV2`, `Insights`/`InsightTicker`/`InsightsScreen`/`InsightsScreenV2`), matching import (`ImportModal`). **Invariati di proposito** (aggregano transazioni e risolvono via getCat/getAcc): filtro storico in `TransactionList`, card dashboard `AccountsCard`/`InvestmentSummaryCard`, `AccountBalanceScreen`, export dati, e il `portfolio` (controvalore/versato) in `App` per non sfasare il patrimonio netto. Integrazione **silenziosa**: nessun toast/banner/modale, nessuna voce in `appInfo.ts` VERSIONS. `(pending)`
- **[Claude]** **Rollout a tutti gli utenti della "Shortcut spese" + annuncio "Novità".** Rimosso il gate admin: la voce **"Spese da iPhone"** in Impostazioni (sezione "Scorciatoie" in v2 / griglia in legacy) è ora visibile a **tutti** (`SettingsScreen` — eliminati `isAdmin`/`isAdminUser`); gli endpoint runtime già funzionavano per qualunque token valido. Il popup **`WhatsNewModal`** non è più admin-only (rimosso il prop `isAdmin` e il gate; mount in `App.tsx` senza `isAdminUser`, import ripulito): annuncia a tutti la voce di changelog con `highlight: true`. Aggiunta release **1.13.0 "Aggiungi spese da iPhone"** (`appInfo.ts`, `highlight: true`, `APP_VERSION` → 1.13.0); spostato `highlight` dalla 1.12.1 alla 1.13.0 così il popup annuncia la scorciatoia. "Già visto" resta in `localStorage` per-versione (nota: per il cross-device si potrà spostare su Firestore). ⚠️ `SUNNY_EXPENSE_SHORTCUT_URL` ancora placeholder: va impostato col link iCloud reale prima del deploy pubblico.
- **[Claude]** **Notifica push "Spesa aggiunta" dopo `addExpense`** (`functions/src/index.ts`): dopo la scrittura, invio best-effort via `sendToUser` (multicast + cleanup token), body coerente con l'app `«emoji categoria» «importo» € in «categoria» da «conto»` (importo assoluto IT senza segno; emoji omessa se assente). Non blocca la risposta; `summary` nel JSON invariata.
- **[Claude]** **Shortcut iOS "Aggiungi spesa" (token, headless) — admin-only (preview).** Nuove Cloud Functions v2 (`europe-west1`, stile `onRequest` + Bearer, **niente onCall** coerente col resto del file): `issueExpenseToken` / `listExpenseTokens` / `revokeExpenseToken` (admin: Firebase ID token + UID admin) e i runtime `getExpenseOptions` (GET) / `addExpense` (POST) autenticati con **token coniato** (middleware condiviso `authExpenseToken`: verifica `expenseTokens/{sha256(token)}`, `revoked`, scope `expenses:write`, rate-limit 30/h → 429, stamp `lastUsedAt`). `addExpense` crea **solo** `type:'expense'` (forzato), importo `Number(String(x).replace(',','.'))` > 0 salvato positivo, categoria/conto risolti per **nome** (label) case-insensitive contro `meta/settings` → id, `description` → nome categoria se vuota, `date` = oggi Europe/Rome, scrittura nella shape `Transaction` esistente (`createdAt` ms, niente id/seriesId/recurring/projected/split, `dropUndefined`); risposta con `summary` pronta per notifica. Token in chiaro mai salvato (solo sha256). `firestore.rules`: `match /expenseTokens/{id} { allow read, write: if false }` (regole `users/{uid}` invariate). UI **solo admin** in Impostazioni → "Shortcut spese" (`ExpenseShortcutSection` + `expenseShortcut.ts`): genera token → copia in clipboard → apre `SUNNY_EXPENSE_SHORTCUT_URL` (placeholder) → istruzioni; lista token con Revoca. Doc completa in `docs/shortcut-spese.md`.
- **[Claude]** **Popup "Novità" (`WhatsNewModal`) — admin-only (preview), per-release a scelta.** Nuovo campo opzionale `highlight?: boolean` su `VersionEntry` (`appInfo.ts`): l'admin accende il popup di una specifica release flaggandone il changelog (impostato su `1.12.1` come esempio). Il modale mostra la voce **più recente** con `highlight===true` solo se `isAdmin` e non già chiusa per quella versione (`localStorage` `sunny_whatsnew_seen_<version>`); un solo CTA "Ho capito". Riusa lo stile premium di `PushPromoSheet`, titolo in DM Serif Display, accento oro. Montato a livello app in `App.tsx` dopo l'auth. Note di rollout (rimuovere il gate admin; spostare il "già visto" su Firestore per il multi-utente) documentate in un commento nel componente.
- **[Claude]** **"Saldo per conto" → saldo a fine periodo + scroll-to-top affidabile.** Navigando nel tempo nella schermata `AccountBalanceScreen` ora cambia anche il **saldo**: sia la **liquidità totale** (hero) sia il **saldo di ogni conto** mostrano il valore a **fine del periodo selezionato** (capped a oggi), non più sempre quello odierno. Il `current` di ogni riga usa ora `aggregateAccountFlow(...).closingBalance` invece di `balanceAsOf(today)`; la liquidità totale è la somma dei closing balance del periodo; l'hero aggiunge la **variazione del periodo** (Δ, gated su `insightDepth`) e una didascalia con la data di riferimento (`al <data>` per periodi passati, "oggi" per il corrente). Il dettaglio conto (`AccountDetailSheet`) era già period-aware: ora la lista combacia col dettaglio. **Fix scroll-to-top**: il reset dello scroll al cambio rotta agiva solo su `window`, ma il vero scroller è il contenitore `#app-scroll` (body/window non scorrono) — quindi su desktop la pagina restava a metà; ora si resetta anche `#app-scroll`. `appInfo.ts` → `1.12.1`.

## 2026-06-16

- **[Claude]** **Rollout a tutti gli utenti delle feature finora admin-only** — disaccoppiata l'identità admin dalla **visibilità**. `featureFlags.ts`: `canUseDetailedInvestments` e `canUseForecastV2` ora `true` per tutti; `isAdminUser` torna un controllo identità **INDIPENDENTE** sull'allowlist UID (`qPtCOJGRrwOZ2EfjxMHwW6ZISXX2`), non delega più a `canUseDetailedInvestments` ed è riservato ai soli percorsi **dati** admin (read `feedback` in `firestore.rules`, **INVARIATO**). Ungate in `App.tsx`: rotta `/account-balance` sempre renderizzata, `/ai-coach` ora gated da `aiEnabled`, `onSeeAccountBalance` sempre passato (Dashboard + V2), `isAdmin={true}` verso `InsightsScreenV2` (FASE 4) e `ForecastV3Screen`, `AICoachWidget` gated da `aiEnabled && aiCoachWidgetEnabled`. `SideNav`: link AI Coach gated solo da `aiEnabled` (rimosso il prop `isAdmin`). Fix entry point Categorie in `DashboardV2` (ora riceve `onSeeCategories`; prima navigava solo inline). **IA disattivata di default**: `aiEnabled` e `aiCoachWidgetEnabled` default `false` (gli utenti che avessero già il campo impostato non vengono forzati); con `aiEnabled=false` nessuna chiamata Gemini (AIDigestCard, AI Coach, widget tutti gated). `canUseUiV2` lasciato invariato (rollout UI separato). `firestore.rules` e logiche server-side privilegiate (push admin feedback, rate-limit AI Coach) invariate. Nessun deploy.

- **[Claude]** **Nuova schermata analitica "Saldo per conto"** (admin, pre-rollout) — speculare a `CategorySpendingScreen` ma centrata sul **saldo** (grandezza cumulativa/stock) invece che sulle spese. Selettore periodo **Mese / 3M / 6M / 12M** con navigazione avanti/indietro (niente futuro), **hero liquidità totale** (somma dei saldi dei conti non-investimento), **lista conti** ordinata per saldo con Δ del periodo colorato. Il tap su un conto apre `AccountDetailSheet` (bottom sheet mobile / side panel desktop) con **curva del saldo** (`AccountBalanceLineChart`, gestisce valori negativi + baseline a 0), **3 KPI** Entrate/Uscite/Investimenti, chip "Trasferimenti netti" solo se ≠ 0, nota Sunny deterministica e movimenti recenti; "Vedi tutti i movimenti" porta a `/transactions?account=<id>` (nuovo filtro conto via URL in `TransactionList`, speculare a `?cat=`). Logica pura **stock-aware** in `accountAnalytics.ts` (apertura del periodo, curva cumulativa che non parte da zero, segni coerenti con `useTransactions`, quadratura sempre verificata, niente futuro, liquidità esclude `isInvestment`) con 7 test in `accountAnalytics.test.ts`. Entry point dalla card "Saldo per conto" in dashboard (`AccountsCard`). Rotta `/account-balance` sotto gate admin. Densità per `insightDepth`. Solo token tema, niente colori cablati.

## 2026-06-15

- **[Claude]** **Nuova schermata analitica "Categorie"** (`CategorySpendingScreen` riscritta): sostituisce donut+lista con un'analisi delle spese per categoria nel tempo. Selettore periodo **Mese / 3M / 6M / 12M** con navigazione tra periodi precedenti/successivi (step a periodo intero, niente futuro), **riepilogo periodo** con totale e variazione vs periodo precedente, **insight Sunny deterministico** (driver dell'aumento / categoria più pesante), **barra composizione** top 4 + Altro, e **ranking a barre orizzontali** (niente bolle, niente donut come grafico principale). Il tap su una categoria apre una **scheda dettaglio** — bottom sheet su mobile, side panel a destra su desktop — con KPI, **grafico a linee** dell'andamento (con toggle Importo/Movimenti/Spesa media in modalità avanzata), nota Sunny e movimenti recenti; "Vedi tutti i movimenti" porta a Movimenti filtrati per categoria (`/transactions?cat=<id>`). La **densità informativa** segue il "Livello di analisi" esistente (Minimal/Media/Smanettone → `insightDepth`): Minimal mostra l'essenziale, Media aggiunge confronti e composizione, Smanettone aggiunge media storica, scostamento, budget usato, proiezione, ticket mediano e concentrazione top-3. Logica pura in `categoryAnalytics.ts` (range/aggregazioni/trend, con test). Nuovi componenti `CategoryTrendLineChart` e `CategoryDetailSheet`. Curata su mobile/desktop e dark/light. La schermata resta sotto la rotta esistente `/category-spending` (gate admin invariato).

## 2026-06-14

- **[Claude]** **Annullate le bolle "Spese per categoria"**: ripristinato il grafico a **donut + lista** sia nella schermata dedicata `CategorySpendingScreen` sia nella card della dashboard `CategoryCard`. Rimosso il componente `CategoryBubbles.tsx` e il filtro per categoria via URL (`?cat=<id>`) in `TransactionList` (non più usato). Il **delta period-aware** per categoria resta (confronto col periodo precedente, testo colorato).

## 2026-06-13

- **[Claude]** **Delta period-aware in CategorySpendingScreen**: ogni riga categoria mostra ora il confronto con il periodo precedente equivalente (mese/trimestre/semestre/anno precedente), con testo colorato — rosso se si è speso di più, verde se meno, grigio se stabile o senza storico. `prevCatSpend` useMemo separato; `prevStart`/`prevEnd`/`prevLabel` calcolati nello stesso useMemo del range corrente.

- **[Claude]** **3 nuovi reminder push** (`functions/src/index.ts`): `remindUpcomingPayments` (18:00 ogni giorno — pagamenti one-off e ricorrenti programmati per domani), `remindInactivity` (21:00 ogni giorno — nessun movimento reale da 5+ giorni, skip primissimi giorni del mese), `remindMonthEnd` (19:00 giorni 28-31 — solo sull'ultimo giorno del mese, riepilogo entrate/uscite/investiti). Toggle UI nelle impostazioni notifiche: "Pagamenti di domani" e "Promemoria inattività". Deploy aggiornato in `deploy-functions.yml`.

## 2026-06-02

- **[Claude]** **Ritocchi UX**: editor categorie/conti (`EditDefSheet`) — selettore icone ora compatto (3 file) con "Mostra altre icone" che si richiude alla selezione; nuove emoji (computer/tech, arrampicata/outdoor, altri sport); **Invio = Salva** da tastiera. Movimenti: in modalità selezione, l'intestazione di ogni gruppo ha un pallino per **selezionare l'intero raggruppamento** in un tocco. — `(pending)`
- **[Claude]** **Movimenti — previsti & ricerca**: le righe "Programmato" ora di default mostrano solo i **prossimi 5 giorni**; orizzonte regolabile dal pannello filtri (sezione "Previsti": 5 giorni / 30 giorni / 3 mesi / tutti / nascondi) con pill rimovibile. **Ricerca potenziata** (`TransactionList`): oltre a descrizione/note/categoria/conto, ora cerca anche in **importo** (grezzo, 2 decimali, forma con virgola e formattato), **data** (ISO + "2 giu 2026" + "giugno 2026"), **conto di destinazione** dei trasferimenti e **tipo**. — `(pending)`
- **[Claude]** **Fix tema chiaro** (form transazione + editor categorie): i chip usavano colori cablati della palette scura — il tipo "Uscita" (`#F5F5F5`, quasi bianco) selezionato spariva sul fondo chiaro, e le categorie non selezionate avevano fondo `#161616`. Ora la barra dei tipi ha fondo tonale (`bg-surface`) + ombra sul selezionato, e i chip non selezionati usano i token di tema (`bg-surface`/`text-secondary`). — `(pending)`
- **[Claude]** **v1.7.0 — Ricorrenze a serie**: le ricorrenze ora "funzionano davvero". Le occorrenze future sono **proiettate lato client** (`sunny/src/shared/recurrence.ts`: `projectOccurrences`/`buildProjectedOccurrences`) e mostrate in anticipo nei Movimenti come righe **"Programmato"** (stile attenuato, icona 🗓️), fino a `until` o a un orizzonte di 12 mesi — senza scrivere nulla su Firestore. Gestione **stile Outlook**: toccando un'occorrenza si apre la **serie** (`TransactionModal` con `seriesEdit`); per le voci già registrate appare uno sheet di scelta `SeriesEditChoiceSheet` ("solo questa" / "tutta la serie"), reso possibile dal nuovo campo `seriesId` che collega template e istanze. I **totali del mese** restano sulle voci realizzate (le proiezioni non gonfiano i subtotali). La **proiezione annuale** (`insightsEngine.ts`) usa ora `max(media storica, ricorrenti riportate al mese)` per uscite e investimenti, così gli impegni fissi non sono sottostimati (senza doppio conteggio). Cloud Function `processRecurringTransactions`: **catch-up loop** che recupera tutte le occorrenze arretrate in un solo run, copia `seriesId` sulle istanze e gestione `daily` corretta (`addPeriod` lato server non avanzava la frequenza giornaliera). `addPeriod` consolidato nell'util condiviso. — `(pending)`
- **[Claude]** **v1.6.0 — Riepilogo AI**: nuova card "Riepilogo AI" nella dashboard che genera 2-3 frasi sulla situazione finanziaria del mese tramite **Google Gemini**. Architettura server-side: Cloud Function callable `generateDigest` (`functions/src/index.ts`) con la chiave Gemini passata come variabile d'ambiente dal GitHub Secret `GEMINI_API_KEY` (mai nel codice). Client (`aiDigest.ts` + `AIDigestCard.tsx`) con **fallback rule-based** se la function non risponde. Nuovo workflow CI dedicato `deploy-functions.yml`. — `(pending)`
- **[Claude]** Fix **livello di analisi**: `InsightsScreen` chiamava `buildInsights()` senza passare `depth`, quindi "Vedi tutti" mostrava sempre tutti gli insight ignorando il profilo scelto (Minimal/Media/Smanettone). Ora il livello è applicato anche lì. `buildInsights()` calcolato una sola volta in `Dashboard` e passato a `InsightTicker` via prop `prebuilt`. — `(pending)`
- **[Claude]** **v1.6.1 — Rifiniture**: modello Gemini aggiornato a `gemini-flash-latest` (`gemini-1.5-flash` era dismesso e faceva sempre scattare il fallback); errori Gemini loggati server-side; riassunto di riserva non mostra più percentuali assurde quando le entrate sono ≈0. — `(pending)`

## 2026-06-01

- **[Claude]** Impostazioni → Generali: nuovo toggle **"Gestione investimenti"** che nasconde l'intera funzionalità (schermata `/investments`, card dashboard Investito + InvestmentSummaryCard, barra "Investito" nella FlowBar, sezione Budget investimenti, tipo "Investimento" nel form transazioni). Il toggle "Includi investito nel patrimonio" scompare automaticamente quando la gestione investimenti è disattivata. — `(pending)`
- **[Claude]** Canale di rilascio **beta** (`APP_CHANNEL` in `appInfo.ts`): badge "BETA" accanto alla versione nelle impostazioni e nota nel registro versioni. Da cambiare in 'stable' quando l'app diventerà ufficiale. — `(pending)`
- **[Claude]** Modello di previsione di fine mese migliorato: da run-rate puro (spese ÷ frazione di mese, instabile a inizio mese) a **"spese finora + (frazione di mese rimasta × media storica)"** — blend pesato sulla confidenza che non esplode a inizio mese, converge al totale reale a fine mese e reagisce comunque ai mesi anomali. Fallback al run-rate guardato senza storico. Spiegazioni aggiornate + 4 nuovi test. — `(pending)`
- **[Claude]** Impostazioni: nuova sezione **"Come funziona"** che spiega in linguaggio semplice tutte le formule (patrimonio, saldo conto, capitale investito, risparmio, previsione fine mese, media storica, tendenze, stagionalità, budget suggerito). Footer con **versione app** (`appInfo.ts`, v1.5.0) e link al **Registro versioni**. — `(pending)`
- **[Claude]** TrendChart: uscite e investimenti ora sono **impilati** — la linea oro (tratteggiata) rappresenta "uscite + investimenti" (totale in uscita), con banda colorata tra le due, confrontabile a colpo d'occhio con le entrate. — `(pending)`
- **[Claude]** Schermata Investimenti: l'allocazione per categoria elenca ora **tutte le categorie di investimento**, anche quelle a €0 (mostrate sbiadite), oltre a eventuali categorie con valore non più presenti tra quelle attive. — `(pending)`
- **[Claude]** Dashboard: la statistica **Risparmio** ora mostra un sottotitolo ("dopo €X investiti" / "entrate − uscite − investimenti") e un indicatore **"!"** quando entrate − uscite − investimenti è negativo, per chiarire perché può risultare < 0 anche con entrate > uscite. Spiegazioni insight ulteriormente semplificate (rimossi termini come "regressione lineare", "pendenza", "deviazione standard" in favore di linguaggio comune). — `(pending)`
- **[Claude]** Previsione di fine mese unificata: nuova funzione condivisa `forecastSavings` usata sia dagli Insight sia dai "Consigli Sunny" del budget, così le due viste non si contraddicono più (prima una poteva dire −500 e l'altra +1000). Modalità "Vedi spese" della dashboard ora elenca **tutti i conti, anche a €0**. Spiegazione insight della proiezione riscritta in linguaggio semplice (niente più formule con ÷). Analisi stagionali/trend limitate agli **ultimi ~18 mesi** (le abitudini molto vecchie non sono più rappresentative). — `(pending)`
- **[Claude]** Nuova schermata **Investimenti** (`/investments`) accessibile dalla dashboard (voce "Investito" e card "Investimenti per categoria" cliccabili): totale investito, allocazione per categoria (donut + % + n. operazioni + flag capitale iniziale), versamenti ultimi 6 mesi, elenco operazioni. Impostazioni: layout desktop migliorato (container più largo, menu e Gestione a griglia di tile con sottotitoli, sezioni constrainte per leggibilità). Su desktop la sidebar torna "Sunny" e il saluto "Buon pomeriggio" appare nella dashboard. Spiegazione insight della proiezione di fine mese resa più chiara (formula esplicita delle uscite previste = uscite finora ÷ frazione di mese trascorsa). — `(pending)`
- **[Claude]** Transazioni: aggiunto **ordina per importo** (più alto / più basso) accanto a ordina per data nel pannello filtri. Selettori filtro-tipo e raggruppamento convertiti in **capsule pill** (stile selettore periodo della dashboard). Insight più intelligenti: l'alert "Sforamento" appare solo dopo metà mese e la proiezione di fine mese solo dopo ~il 15% del mese (niente più stime gonfiate da una singola spesa a inizio mese). Nuovo **saldo iniziale per le categorie di investimento** (capitale già investito prima di Sunny): editabile come per i conti, sommato a patrimonio investito e riepilogo per categoria. — `(pending)`
- **[Claude]** Budget più leggibile: nuova card **panoramica del mese** (`BudgetOverview`) con barra impilata Entrate/Spese/Investimenti/Risparmio, spaziatura più ariosa tra le sezioni. Suggerimenti di budget ora **consapevoli della stagionalità** (`suggestBudgets` alza il budget di una categoria al livello storico del mese corrente, es. regali a dicembre) + banner che lo spiega. Nuove funzioni `seasonalMonthlyAverage`/`seasonalHint` con test. — `(pending)`
- **[Claude]** Dashboard ridisegnata: saluto+nome ("Buongiorno, Luca") al posto di "Sunny" nell'header e nella sidebar, rimossa la foto profilo. Navigatore periodo con frecce ‹ › per scorrere i mesi/periodi passati (+ "Oggi" per tornare). Nuova card **FlowBar** (Entrate · Uscite · Investito a confronto), TrendChart ora a 3 linee con l'investito, nuova card **Investimenti per categoria** (donut + percentuali), "Saldo per conto" mostra 3 conti con "Mostra tutti". Gli insight in dashboard sono ora un **banner orizzontale scorrevole** di card compatte (stile telegiornale) che aprono la spiegazione al tap. — `(pending)`
- **[Claude]** Insight v3: ogni insight ha un pulsante "i" che apre un pannello (`InsightDetailSheet`) con spiegazione strutturata (cosa indica · come è calcolato · su quali dati) e un mini-grafico a barre con linea di riferimento. Nuova categoria **Stagionalità**: confronti year-over-year (stesso mese anno scorso) e rilevamento di categorie che storicamente salgono in un dato mese (es. regali a dicembre), con anticipo sul mese successivo. — `(pending)`
- **[Claude]** Insight v2: motore riscritto con 22 analisi raggruppate in 5 categorie (Priorità · Previsione · Tendenze · Abitudini · Questo mese). Nuove analisi: slope lineare spese/risparmio su 6 mesi, tasso investimento sul reddito, proiezione annuale, miglior mese anno, categoria in crescita rapida (3 mesi), weekend vs feriali, giorni senza spese, concentrazione Pareto top-3 categorie, spesa anomala (outlier vs media storica categoria), volatilità mensile (std dev), cash flow efficiency, diversità fonti di reddito. InsightsScreen rinnovata con strip riassuntiva entrate/uscite/risparmio e sezioni per categoria con contatore. — `(pending)`
- **[Claude]** Transazioni: nuovo pannello filtri accanto alla ricerca (ordina per data più/meno recenti + selezione periodo: tutto/ultimo mese/3/6 mesi/anno, con pill rimovibile e chiusura via Esc). Gruppi comprimibili cliccando sull'intestazione (mese, conto o categoria), con chevron, conteggio e pulsante "Comprimi/Espandi tutti". UI dei selettori uniformata: filtro tipo (Tutte/Uscita/Entrata…) e raggruppamento (Per mese/conto/categoria) ora condividono lo stesso stile segmented, con pallino colore per tipo. — `(pending)`
- **[Claude]** Dashboard: rimossa la sezione "Recenti". AccountsCard: il riquadro "Saldo/Spese per conto" non sparisce più passando a "Vedi spese" quando non ci sono spese nel periodo (header e toggle restano, con messaggio vuoto). Transazioni: nuovo raggruppamento "Per categoria" oltre a mese e conto. — `(pending)`
- **[Claude]** Insight potenziati: nuovo motore (`insightsEngine.ts`, con test) che aggiunge previsione di fine mese, previsione entrate e investimenti sulla base della media storica (ultimi 3 mesi attivi), voce di spesa più pesante, spese vs media. Nuovo setting in Generali per includere o no il capitale investito nel patrimonio netto (`includeInvestments`), con calcolo aggiornato. — `(pending)`
- **[Claude]** Aggiunte 60 emoji alla palette categorie: sport (⚽🏊🚴…), natura/montagna (🌲🏔️🌸…), shopping (🏪👠💎…), cibo/ristoranti (🍕🍴🍷…). — `(pending)`
- **[Claude]** Saldo iniziale nascosto per il conto Investimenti (`isInvestment: true`): il campo appare solo per conti correnti, risparmio, carta di credito e contanti. — `(pending)`
- **[Claude]** Fix sicurezza: chiave Firebase rimossa dal codice sorgente e spostata in variabili d'ambiente Vite (`VITE_FIREBASE_*`). `sunny/.env.local` gitignored. Workflow di deploy aggiornato per passare le `VITE_FIREBASE_*` alla build (chiave API via GitHub Secret `VITE_FIREBASE_API_KEY`, valori pubblici inline). — `4b8e5b3`

---

## 2026-05-31

- **[Claude]** Navbar riorganizzata in layout simmetrico `Home · Insight · (+) · Budget · Movimenti` e nuova sezione **Insight** dedicata (`/insights`); la Dashboard mostra un'anteprima di 3 insight con link "Vedi tutti". Aggiunte emoji vestiti da uomo (👔 👕 👖) alla palette categorie. — `6d9b1bb`
- **[Claude]** Nuova sezione **Budget** (copilota finanziario): obiettivo di risparmio mensile, budget per categoria con stati calmi (normale / vicino al limite / sopra il previsto), budget suggerito da Sunny, "Consigli Sunny". Persistenza in localStorage, dati demo di fallback, componente `ProgressBar` riutilizzabile. — `b812754`
- **[Claude]** Redesign UI verso estetica luxury fintech stile Trade Republic: sfondo quasi nero (#050505), card appena distinguibili, oro desaturato (#C8A05A), rimozione di glow/gradienti/blur, logo semplificato. — `099909b`
- **[Claude]** Semplificata l'inizializzazione di Firestore per risolvere la sincronizzazione cross-device. — `a63811f`

## 2026-05-30

- **[Claude]** Spostato il backdrop del dropdown fuori dallo stacking context dell'header (fix z-index avatar). — `1e9b8ca`
- **[Claude]** Layout desktop responsive con sidebar fissa (220px) e bottom nav solo su mobile. — `56420a3`
- **[Claude]** Impostazioni ristrutturate in tre voci di menu navigabili (Generali / Gestione / Dati). — `18510b0`
- **[Claude]** Saldo iniziale per conto, fix trasparenza glass in dark mode, restructure impostazioni. — `b62059d`
- **[Claude]** Vari fix tema chiaro/scuro: toggle, glass-nav/header in light mode, persistenza senza stale-closure. — `d7e386e`, `15185b0`, `e34a4ec`, `58d8ff6`
